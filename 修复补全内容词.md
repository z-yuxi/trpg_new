# 修复/补全内容

> 复制下方内容，连同 12 份设计文档 + 当前项目代码一起发送给 AI。
> 建议按批次执行：每次发送一个批次的提示词 + 该批次涉及的文件。

---

```
你是一名 TypeScript 全栈工程师。我有一个已搭建好骨架的 TRPG 平台项目（pnpm monorepo：packages/shared + packages/server + packages/client），需要你按以下 20 个批次逐步修复和补全。

## 项目现状

已完成且质量合格（不要改动）：
- 规则引擎：dice-lexer / dice-parser / dice-evaluator / formula-evaluator / executor / registry / atoms
- 数据库 migration（001_init.ts）：14 张表，结构正确
- 共享类型（packages/shared/src/types/index.ts）：所有实体 interface 已定义
- Socket.IO 事件类型（packages/shared/src/types/events.ts）
- 雪花 ID（packages/shared/src/utils/snowflake.ts）
- ILF 工具（packages/shared/src/utils/ilf.ts）
- CSON 工具（packages/shared/src/utils/cson.ts）
- 认证中间件（packages/server/src/middleware/auth.ts）
- 认证服务（packages/server/src/services/auth-service.ts）
- 认证路由（packages/server/src/routes/auth.ts）
- Redis 连接 + 环形缓冲区（packages/server/src/db/redis.ts, utils/ring-buffer.ts）

需要修复/补全的问题清单（按优先级排列）：

### 🔴 P0：阻断性问题
1. log-export-service.ts 引用了不存在的表（campaign_members, character_instances, character_cards）和不存在的字段（campaigns.created_by 应为 gm_user_id, campaigns.title 应为 name, scenes.description 不存在, chat_messages.seq 不存在, chat_messages.speaker 不存在）
2. Socket chat-handler.ts 中 scene_id 永远为空字符串（第66行），story_time 永远为 null（第72行）
3. Socket 的 gm_advance_time / request_move / gm_approve_move / gm_reject_move 全是 console.log 占位
4. routes/characters.ts 所有端点返回 501
5. routes/recruitment.ts 所有端点返回 501
6. routes/campaigns.ts 中 7 个端点返回 501 或空数组

### 🟡 P1：前端全是假数据
7. views/Login.vue 使用 mock-token 而非调用真实 auth API
8. views/Home.vue 硬编码了假战役名、假用户名、假活动流
9. views/Personal.vue 硬编码了假用户（UID 1000001, 1024 币）
10. views/MyCampaigns.vue 硬编码了假战役列表，创建/加入函数为空
11. views/CreatorDashboard.vue 硬编码了假收入、假订单
12. views/Room.vue scenes/characters/commands 全是空 mock 数组，joinRoom 传了空 characterId
13. views/asset/DiscoverTab.vue + MyAssetsTab.vue 硬编码了假规则包
14. views/community/RecruitmentBoard.vue 硬编码了假招募帖
15. views/CharacterEditor.vue 保存函数只有 console.log
16. views/Community.vue 发帖函数只有 console.log

### 🟢 P2：缺失功能
17. 全局没有返回按钮/面包屑导航
18. GMConsole.vue 线索管理标签页为空占位
19. 会员/订阅系统无 UI 和 API
20. reconnection-handler.ts 中 your_state 为空对象

## 批次执行计划

每个批次我会告诉你"执行批次 N"，你需要：
1. 读取我提供的当前文件内容
2. 输出修改后的完整文件（不要省略未改动的部分）
3. 如果需要新建文件，给出完整内容和路径

---

### 批次 01：修复 log-export-service.ts

读取以下文件获取上下文：
- packages/server/src/services/log-export-service.ts（当前有错误的版本）
- packages/server/src/db/migrations/001_init.ts（真实表结构）
- packages/shared/src/types/index.ts（ChatMessage, Campaign 等 interface）

修复要求：
- `campaigns` 表权限字段：`created_by` → `gm_user_id`，`title` → `name`
- 删除对 `campaign_members` 表的查询——改为查 `character_scene_states` 表（字段：character_id, campaign_id）+ 关联 `character_sheets`（字段：id, name, user_id）+ 关联 `users`（字段：id, nickname）
- `scenes` 表没有 `description` 字段——删除该 select
- `chat_messages` 表没有 `seq` / `speaker` / `story_time_day` 等拆分字段——消息按雪花 ID `id` 排序（`orderBy('id', 'asc')`），story_time 是 JSON 字段（需要 `JSON_EXTRACT` 或读出后 JSON.parse），sender 信息通过 `sender_user_id` 关联 `users.nickname`，`sender_character_id` 关联 `character_sheets.name`
- `msg_type` → `message_type`，骰子信息在 `metadata` JSON 字段中

验收：TypeScript 编译通过，函数签名不变。

---

### 批次 02：修复 chat-handler.ts 的 scene_id 和 story_time

读取以下文件：
- packages/server/src/socket/chat-handler.ts
- packages/server/src/db/index.ts

修复要求：
- 在 `chat_message` 事件处理中，scene_id 从 `character_scene_states` 表查询当前角色所在场景：`db('character_scene_states').where({ character_id: characterId, campaign_id: campaignId }).select('current_spatial_scene_id').first()`。如果角色未绑定场景，从客户端传入的 `data.scene_id` 取值
- story_time 从 `campaigns` 表查询当前全局故事时间：`db('campaigns').where({ id: campaignId }).select('global_story_time').first()`，然后 `JSON.parse(row.global_story_time)`
- 客户端发送 chat_message 时应携带 scene_id 字段（events.ts 中的 ClientToServerEvents 已定义）

验收：编译通过，发送消息后数据库中 scene_id 和 story_time 不再为空。

---

### 批次 03：实现 Socket GM 操作 handler

读取以下文件：
- packages/server/src/socket/chat-handler.ts
- packages/server/src/services/campaign-service.ts
- packages/shared/src/types/events.ts
- packages/shared/src/types/index.ts（StoryTime, ScheduledMove）

实现以下 4 个 handler（替换 console.log）：

**gm_advance_time(data: { campaign_id, new_time: StoryTime })**：
1. 验证 socket.data.userId 是该 campaign 的 gm_user_id
2. 查询当前 global_story_time，验证 new_time > 当前时间（按 day→hour→minute 比较）
3. 更新 `campaigns` 表的 `global_story_time`
4. 查询 `scheduled_moves` 中 status='pending' 且 execute_at_story <= new_time 的记录
5. 对每条到期的 move：更新 `character_scene_states.current_spatial_scene_id`，插入 `position_history` 记录，更新 move status='executed'
6. 广播 `time_advanced` 事件，payload 包含 new_time 和 executed_moves 数组

**request_move(data: { campaign_id, to_scene_id })**：
1. 在 `scheduled_moves` 表插入一条 status='pending' 的记录
2. 向 GM 发送 `move_requested` 事件

**gm_approve_move(data: { move_id })**：
1. 更新 `scheduled_moves` 状态为 'approved'
2. 通知请求者 `move_approved`

**gm_reject_move(data: { move_id })**：
1. 更新 `scheduled_moves` 状态为 'cancelled'
2. 通知请求者 `move_rejected`

约束：StoryTime 比较逻辑——先比 day，再比 hour，再比 minute。不要引入额外依赖。

验收：编译通过。

---

### 批次 04：实现 characters 路由

读取以下文件：
- packages/server/src/routes/characters.ts（当前全 501）
- packages/server/src/services/character-sheet-service.ts（已有 CRUD 方法）
- packages/server/src/middleware/auth.ts（authMiddleware）
- packages/shared/src/types/index.ts（CharacterSheet）
- packages/shared/src/utils/cson.ts（parseCSON, serializeCSON）

实现要求：
- 所有路由加 `authMiddleware`
- POST `/` → 调用 characterSheetService.create({ ...body, user_id: req.user.id })
- GET `/` → 调用 characterSheetService.findByUserId(req.user.id)
- GET `/:id` → 调用 characterSheetService.findById(id)，验证 user_id 匹配
- PUT `/:id` → 调用 characterSheetService.update(id, body)，验证所有权
- DELETE `/:id` → 调用 characterSheetService.delete(id)，验证所有权
- POST `/import` → 调用 parseCSON(req.body.cson_text)，然后 characterSheetService.create(parsed)
- POST `/:id/export` → 查出角色卡，调用 serializeCSON(sheet)，返回 { cson_text }

验收：编译通过。

---

### 批次 05：实现 recruitment 路由

读取以下文件：
- packages/server/src/routes/recruitment.ts（当前全 501）
- packages/server/src/services/recruitment-service.ts（已有 RecruitmentService）
- packages/server/src/middleware/auth.ts
- packages/shared/src/types/index.ts（RecruitmentPost）

实现要求：
- POST `/` → recruitmentService.create({ ...body, poster_id: req.user.id })
- GET `/` → recruitmentService.list(query.status, query.type)，支持分页 ?page=1&limit=20
- GET `/:id` → recruitmentService.findById(id)
- PUT `/:id` → recruitmentService.update(id, body)，验证 poster_id 匹配

验收：编译通过。

---

### 批次 06：补全 campaigns 路由的 501 端点

读取以下文件：
- packages/server/src/routes/campaigns.ts
- packages/server/src/services/campaign-service.ts
- packages/shared/src/types/index.ts（Scene, CampaignNpc, ChatMessage, PositionHistory）

实现要求（替换 501 / 空数组的端点）：
- POST `/:id/scenes` → db('scenes').insert({ id: generateId(), campaign_id, ...body })
- POST `/:id/scenes/connections` → db('scene_connections').insert(...)
- GET `/:id/scenes/connections` → db('scene_connections').where({ campaign_id: id })
- POST `/:id/npcs` → db('campaign_npcs').insert({ id: generateId(), campaign_id, created_by: req.user.id, ...body })
- GET `/:id/npcs` → db('campaign_npcs').where({ campaign_id: id })
- PUT `/:id/npcs/:npcId` → db('campaign_npcs').where({ id: npcId }).update(body)
- GET `/:id/messages` → db('chat_messages').where({ campaign_id: id }).orderBy('id', 'asc').limit(50)，支持 ?after_id= 和 ?scene_id= 查询参数
- GET `/:id/round-state` → db('campaign_round_state').where({ campaign_id: id }).first() || null
- GET `/:id/position-history` → db('position_history').where({ campaign_id: id }).orderBy('created_at', 'desc')

验收：编译通过。

---

### 批次 07：修复 Login.vue 接入真实 API

读取以下文件：
- packages/client/src/views/Login.vue
- packages/client/src/stores/auth-store.ts
- packages/server/src/routes/auth.ts（了解 API 地址和参数格式）

修复要求：
- 删除 mock-token 逻辑
- 注册：POST `/api/auth/register` body: { phone, password, nickname }
- 登录：POST `/api/auth/login` body: { phone, password }
- 成功后将 response.token 和 response.user 存入 authStore
- 失败时显示 API 返回的错误信息
- 登录成功后 router.push(route.query.redirect || '/')

验收：能正常注册、登录，token 存入 localStorage。

---

### 批次 08：清理 Home.vue 接入 API

读取以下文件：
- packages/client/src/views/Home.vue
- packages/client/src/stores/campaign-store.ts

修复要求：
- 删除所有 mock 数据（假战役名、假用户名、假活动流）
- 我的最近战役：onMounted 调用 GET `/api/campaigns`，取前 3 条
- 推荐规则包：onMounted 调用 GET `/api/rulesets`，取前 4 条
- 活动流：暂时显示为空列表 + "暂无动态"空状态（不要编造假数据）
- 如果 API 报错，显示错误提示而不是假数据

验收：页面不显示任何硬编码的假名字。

---

### 批次 09：清理 Personal.vue + MyCampaigns.vue

读取以下文件：
- packages/client/src/views/Personal.vue
- packages/client/src/views/MyCampaigns.vue
- packages/client/src/stores/auth-store.ts

修复要求：

**Personal.vue**：
- 删除 mockUser，改为从 authStore 读取当前用户信息
- 如果 authStore 中用户信息不完整，onMounted 调用 GET `/api/users/me` 补全
- 订阅类型、余额、创作者等级等字段从真实用户数据读取

**MyCampaigns.vue**：
- 删除硬编码假战役
- onMounted 调用 GET `/api/campaigns` 获取列表
- 创建战役：调用 POST `/api/campaigns`，成功后刷新列表
- 加入战役：调用 POST `/api/campaigns/join` body: { code }，成功后跳转

验收：两个页面不显示任何硬编码数据。

---

### 批次 10：清理 CreatorDashboard + AssetLibrary + Community

读取以下文件：
- packages/client/src/views/CreatorDashboard.vue
- packages/client/src/views/asset/DiscoverTab.vue
- packages/client/src/views/asset/MyAssetsTab.vue
- packages/client/src/views/community/RecruitmentBoard.vue
- packages/client/src/views/Community.vue

修复要求：
- **CreatorDashboard**：删除假收入/假订单，显示"暂无数据"空状态（创作者后台的交易系统不在当前版本范围）
- **DiscoverTab**：删除假规则包，onMounted 调用 GET `/api/rulesets?status=published`
- **MyAssetsTab**：删除假数据，onMounted 调用 GET `/api/rulesets?mine=true`
- **RecruitmentBoard**：删除假招募帖，onMounted 调用 GET `/api/recruitment`
- **Community.vue**：submitPost 调用 POST `/api/recruitment`

验收：无硬编码假数据。

---

### 批次 11：修复 Room.vue 数据加载

读取以下文件：
- packages/client/src/views/Room.vue
- packages/client/src/socket/socket-client.ts
- packages/client/src/stores/campaign-store.ts
- packages/client/src/stores/auth-store.ts

修复要求：
- 删除所有 mock 数组
- onMounted 流程：
  1. 从 route.params.id 获取 campaignId
  2. GET `/api/campaigns/${campaignId}` → 存入 campaignStore
  3. GET `/api/campaigns/${campaignId}/scenes` → 存入 campaignStore.scenes
  4. GET `/api/campaigns/${campaignId}/npcs` → 存入本地 ref
  5. 获取当前用户在该 campaign 中绑定的 characterId（从 character_scene_states 或通过额外 API）
  6. socketClient.connectRoom()，传入真实 characterId
  7. socketClient.joinRoom(campaignId, characterId)
- commands 列表从 ruleset 的 commands 字段读取（GET `/api/rulesets/${campaign.ruleset_id}`）
- onUnmounted 调用 leaveRoom + disconnect

验收：进入房间后能从 API 加载场景列表和 NPC 列表。

---

### 批次 12：添加全局返回导航

读取以下文件：
- packages/client/src/layouts/MainLayout.vue
- packages/client/src/router/index.ts

修复要求：
- 在 MainLayout.vue 的顶部栏中，router-link 首页 logo 左侧添加返回按钮（仅在非首页路由时显示）
- 返回按钮调用 router.back()
- 在顶部栏中间显示当前页面标题（从 route.meta.title 读取）
- 在 router/index.ts 的每个路由中添加 meta: { title: '页面名' }：
  - `/` → '首页'
  - `/assets` → '素材广场'
  - `/campaigns` → '我的战役'
  - `/community` → '社区'
  - `/personal` → '我的'
  - `/room/:id` → '跑团房间'（使用 RoomLayout 不需要返回按钮）
  - `/login` → '登录'
  - `/characters/editor` → '角色编辑'
  - `/creator` → '创作台'
  - `/settings` → '设置'

验收：每个页面顶部显示标题，非首页显示返回按钮。

---

### 批次 13：修复 reconnection-handler.ts

读取以下文件：
- packages/server/src/socket/reconnection-handler.ts
- packages/shared/src/types/index.ts（CharacterSceneState, StoryTime）

修复要求：
- `your_state` 不再为空对象 `{}`，改为查询 `character_scene_states` 表获取该角色的完整状态：
```
  const state = await db('character_scene_states')
    .where({ character_id, campaign_id })
    .first();
  ```
- `global_time` 从 `campaigns.global_story_time` 正确解析：
  ```
  const campaign = await db('campaigns').where({ id: campaign_id }).select('global_story_time').first();
  const globalTime = JSON.parse(campaign.global_story_time);
  ```
- 将 your_state 和 global_time 正确填入 missed_messages 事件的 payload

验收：编译通过。

---

### 批次 14：实现 CharacterEditor.vue 保存功能

读取以下文件：
- packages/client/src/views/CharacterEditor.vue
- packages/shared/src/types/index.ts（CharacterSheet）

修复要求：
- save() 函数调用 POST `/api/characters` (新建) 或 PUT `/api/characters/${id}` (编辑)
- 根据 route.query.id 判断是新建还是编辑
- 编辑模式 onMounted 调用 GET `/api/characters/${id}` 加载数据
- 保存成功后 router.push('/characters') 或显示成功提示
- 删除示例默认值，表单初始状态为空

验收：能创建和编辑角色卡。

---

### 批次 15：GMConsole.vue 线索管理

读取以下文件：
- packages/client/src/components/room/GMConsole.vue
- packages/client/src/components/ClueCard.vue
- packages/client/src/styles/text-art.css（8 种 CSS 主题样式）

修复要求：
- 替换"线索库（Step 35 完善）"占位
- 实现线索标签页内容：
  - 线索列表（从 campaign 的自定义数据或未来 API 读取，当前用本地 ref 管理）
  - 添加线索表单：标题、内容文本、选择 CSS 主题（river/blur/fragment/wave/ancient/blood/ash/cyber）
  - 发送线索按钮：通过 socket 发送 message_type='clue_card' 的消息，metadata 中包含 { theme, title }
  - 线索预览：使用 ClueCard 组件 + text-art.css 主题类名

验收：GM 能创建线索并选择文字艺术主题发送到聊天区。

---

### 批次 16：修复 TTag.vue 使用设计令牌

读取以下文件：
- packages/client/src/components/base/TTag.vue
- packages/client/src/styles/tokens.css

修复要求：
- 将硬编码的颜色（#f1f5f9, #dbeafe, #dcfce7, #fef9c3, #fee2e2）替换为 CSS 变量引用
- primary 使用 var(--color-primary)，success 使用 var(--color-success)，以此类推
- 确保在日/夜模式下颜色都正确

验收：切换日夜模式时 TTag 颜色跟随变化。

---

### 批次 17：补全 theme-day.css

读取以下文件：
- packages/client/src/styles/theme-day.css
- packages/client/src/styles/theme-night.css

修复要求：
- theme-day.css 当前只覆盖了 Element Plus 变量，缺少对自定义 token 变量的覆盖
- 对照 theme-night.css 中定义的所有 CSS 变量，在 theme-day.css 中补全对应的日间模式值
- 日间模式：浅色背景（#f8fafc 系列）、深色文字（#1e293b 系列）

验收：切换日/夜模式时所有颜色正确切换。

---

### 批次 18：JWT 安全加固

读取以下文件：
- packages/server/src/services/auth-service.ts

修复要求：
- 如果 JWT_SECRET 环境变量未设置，启动时抛出错误并终止进程，不使用默认值
- 在 server 入口（app.ts 或 server.ts）启动前检查必要的环境变量：JWT_SECRET, DB_HOST, REDIS_URL

验收：不设置 JWT_SECRET 时服务无法启动。

---

### 批次 19：前端 HTTP 请求统一封装

修复要求：
- 创建 packages/client/src/utils/api.ts
- 封装 axios 或 fetch，统一处理：
  - baseURL: 从环境变量读取或默认 '/api'
  - 请求拦截器：自动附加 Authorization: Bearer ${authStore.token}
  - 响应拦截器：401 时清除 token 并跳转登录页
  - 错误统一格式化
- 导出 `api.get()`, `api.post()`, `api.put()`, `api.delete()`
- 批次 07~14 中的 fetch 调用改为使用此封装（在提示词中说明即可，实际修改在各批次中完成）

验收：编译通过，api.ts 导出可用。

---

### 批次 20：删除 GMConsole.vue 重复函数

读取以下文件：
- packages/client/src/components/room/GMConsole.vue

修复要求：
- 删除 `advanceTime()` 和 `advance()` 中的重复函数，只保留一个
- 确保模板中的调用引用正确

验收：编译通过，无重复函数。

---

## 执行规则

1. **每次只执行一个批次**。我会说"执行批次 N"，你只做那个批次的事。
2. **输出完整文件**，不要用"其余不变"省略——我需要直接复制粘贴替换。
3. **不要添加文档未提及的功能**。如果某个功能当前版本不做，显示"暂无数据"空状态即可。
4. **不要修改标记为"已完成且质量合格"的文件**，除非当前批次明确要求。
5. **保持现有的类型定义和 import 路径不变**——shared/types/index.ts 和 events.ts 是真相源。
  ```
