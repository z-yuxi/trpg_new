## 《项目启动与验收指南》生成提示词（完整版）

我的 TRPG 通用平台项目代码已通过约 40 个步骤全部生成完毕，所有代码文件位于当前工作目录中。

现在请为我生成一份独立的《项目启动与验收指南》Markdown 文档，供我（产品负责人，不懂代码）在 **Ubuntu 22.04 / 24.04 LTS** 系统上首次启动和验收项目时使用。

指南必须包含以下七个部分，不得省略或简化：

### 1. 系统环境准备
列出 Ubuntu 下必需的软件及安装命令：
- Node.js 20.x（使用 NodeSource 仓库或 nvm，给出具体命令）
- pnpm（使用 npm 全局安装，给出具体命令）
- MySQL 8.0（提供两种可选方案：APT 仓库安装 或 Docker 运行，分别给出命令）
- Redis 7（提供两种可选方案：APT 仓库安装 或 Docker 运行，分别给出命令）
- Git（使用 apt install 安装）
每个软件附验证命令（如 `node -v`，并说明预期输出示例）。

### 2. 一键启动脚本（可选）
提供一个可直接执行的 bash 脚本内容，命名为 `start.sh`。该脚本应包含：
- 检查 Node.js、pnpm、MySQL、Redis 是否已安装并可访问
- 自动执行 `pnpm install` 安装依赖
- 自动执行数据库迁移命令（需先确认 MySQL 运行中）
- 同时启动前后端开发服务（后台运行或使用 concurrently 等工具）
- 输出访问地址 http://localhost:5173

### 3. 手动启动步骤
假设我已将代码放在 `/home/ubuntu/trpg-platform` 目录下，请写出从该目录开始，到浏览器访问页面的**完整命令行序列**。每一步一行，附带注释说明该步的作用。包括：
- 进入项目目录
- 安装依赖
- 配置环境变量（如有需要）
- 启动 MySQL 和 Redis（如果尚未运行）
- 执行数据库迁移
- 启动后端开发服务器
- 启动前端开发服务器
- 提示浏览器访问地址

### 4. 功能验收清单
按 **Phase 分组** 列出验收场景。格式为三列表格：
| 验收场景 | 操作步骤 | 期望结果 |
|----------|----------|----------|
覆盖全部核心功能模块，至少包括：
- Phase 1：骰子引擎（/r 指令、含修正值、含优势劣势等）、P0 组件渲染
- Phase 2：用户注册/登录、角色卡模板 CRUD
- Phase 3：创建/加入跑团房间、实时聊天、Socket 断线重连
- Phase 4：日夜模式切换、图标库显示
- Phase 5：广场页面、个人中心页面
- Phase 6：跑团房间布局（左栏、聊天区、助理台、GM 控制台）
- Phase 7：线索系统、角色卡编辑器（CSON 导入导出）、日志导出（ILF 格式）
- Phase 8：移动端适配（浏览器移动模拟器下布局变化）、PWA 安装提示

### 5. 文件目录速查表
基于**当前真实项目目录结构**，列出主要功能与对应核心文件路径的映射表，格式为：
| 功能域 | 核心文件路径 | 说明 |
|--------|-------------|------|
| 骰子表达式解析 | `packages/shared/src/dice/` | 词法分析、语法树、求值器 |
| …… | …… | …… |
请覆盖：骰子引擎、P0/P1 组件、规则校验、数据库模型、HTTP API 路由、Socket 事件处理、前端页面组件、角色卡编辑器、移动端样式等。

### 6. 常见问题排查
针对 Ubuntu 环境常见的启动失败场景，用表格给出：
| 错误现象 | 可能原因 | 解决命令或操作 |
|----------|----------|----------------|
| `pnpm: command not found` | pnpm 未安装 | `npm install -g pnpm` |
| `connect ECONNREFUSED 127.0.0.1:3306` | MySQL 未启动 | `sudo systemctl start mysql` 或 Docker 命令 |
| `Redis connection error` | Redis 未启动 | `sudo systemctl start redis` 或 Docker 命令 |
| 端口 3000 或 5173 被占用 | 已有进程占用 | `lsof -i :3000` 查看并 kill |
| 页面白屏，控制台 404 | 后端未启动或路由错误 | 检查后端日志 |
| 依赖安装失败 | 网络或权限问题 | 使用国内镜像源或 `sudo` |

### 7. 交付交接清单
提供一段**可直接复制粘贴的话术模板**，供我完成验收后，将项目移交给开发团队时使用。话术需包含：
- 项目已通过功能验收的声明
- 启动命令的简要说明
- 主要技术栈提醒
- 核心代码目录指引
- 后续开发工作的建议切入方式

### 重要约束
- 语言必须清晰、指令式，避免技术术语，面向非技术人员。
- 所有路径和命令必须基于**当前已生成的真实项目结构**，不得凭空编造。
- 验收清单中的操作步骤必须具体到“在哪个页面、点击哪个按钮、输入什么内容”。
- 生成的《项目启动与验收指南》是一份独立的 Markdown 文档，我将单独保存为 `项目启动与验收指南.md`。
