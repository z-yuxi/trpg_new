{
  "name": "trpg-platform",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter @trpg/server dev",
    "dev:client": "pnpm --filter @trpg/client dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
