> **设计系统版本**：v1.1（秩序优先）  
> **核心原则**：扁平、中性、高对比度、SVG图标、无玻璃拟态、无装饰纹理  
> **版本说明**：V3.0 基于V2.3优化，整合开团简化、观战融合、邀请闭环等UX改进。  
> **状态**：定稿。
>
> **⚠️ 文档定位**：本文档定义各页面的**核心模块、关键交互规则与组件权限**，不维护逐像素的 ASCII 布局图。具体布局由 AI 基于本规则与《附录 B：设计令牌》自动生成。若本规则与《产品设计.md》冲突，以《产品设计.md》为准。
---

## 一、整体页面架构（电脑端优先，移动端PWA适配）

### 底部导航（5个）

| 导航      | 页面定位      | 核心任务                  | 主要模块                  |
| ------- | --------- | --------------------- | --------------------- |
| **首页**  | 内容发现与快速组车 | 浏览推荐、快速找团/带团          | 快速组车卡片、为你推荐、组车动态、新手入门 |
| **资产库** | 数字资产中心    | 浏览/购买模组规则、管理我的资产、创作内容 | 发现（集市）、我的（资产+创作）      |
| **我的团** | 跑团入口与管理   | 进入我的房间、创建房间、加入房间      | 我的房间（唯一列表）            |
| **社区**  | 社交与约局     | 双向组车、动态、讨论            | 招募板（想玩/想带/我的）、动态、讨论区  |
| **个人**  | 个人中心与设置   | 个人资料、通知、私信、账号         | 个人主页编辑、通知、私信、设置、创作者后台 |

---

## 二、各页面详细设计

### 2.1 首页（冷启动友好 + 强组车引导）

**定位**：用户打开平台的第一站，展示**永不空**的内容，并强力引导用户参与组车。

**布局**：两列（主内容区 2/3，右侧边栏 1/3）

**主内容区**：
#### 2.1.1 快速组车卡片

**功能定位**：为未明确目标的用户提供核心入口。

**包含入口**：
- **找团玩**：跳转至社区-招募板的“找团玩”Tab。
- **当 KP**：跳转至社区-招募板的“当KP”Tab。
- **发布求组帖**：直接打开发布求组帖的表单模态框。
- **发布招募帖**：直接打开发布招募帖的表单模态框。
- **可观战的团**：跳转至招募板并自动筛选“可观战”的招募帖。

**交互规则**：
- 所有入口点击后执行对应跳转或打开模态框。
- 未登录用户点击时，先弹出登录/注册模态框。
#### 2.1.2 为你推荐（卡片网格，3列）
- 基于用户偏好推荐的**模组**和**规则包**混合展示
- 冷启动时展示平台精选的免费/热门内容（官方预置10+个）
- 卡片：封面、标题、类型标签（模组/规则）、作者、价格
- 操作：`[开团]` → 触发简化开团流程（见2.6节）

#### 2.1.3 组车动态（混合展示，横向滚动或列表）
- 展示最新**招募帖**和**求组帖**各3条，带标签区分：
  - `[招募]` 蓝色标签：标题、GM、规则、人数进度
  - `[求组]` 绿色标签：玩家昵称、期望规则、可参与时间
- 点击卡片跳转社区-招募板对应帖子详情
- 冷启动时由官方/运营填充示例内容

**右侧边栏**：

#### 2.1.4 新手入门指南（固定模块）
- 图文/视频教程、快速创建角色卡、第一个模组推荐
- 每个条目可点击跳转帮助中心

#### 2.1.5 平台动态（精简）
- 平台公告、新功能上线、优秀创作者作品推荐



---

### 2.2 资产库（两层结构 + 状态标签优化）

**页面结构**：一级Tab：`发现` / `我的`

#### 2.2.1 发现（原“浏览”）
- **二级Tab**：模组集市 / 规则集市
- **模组集市**：
  - 三级Tab：推荐、新上、**公示**、关注
  - 筛选栏：规则、题材、难度、人数、风格、排序
  - 卡片网格（4列），**每个卡片增加状态标签**：
    - **未拥有**：显示价格（免费 / ¥29），操作按钮 `[购买]` 或 `[免费获取]`
    - **已拥有**：显示绿色标签“已在库中”，操作按钮 `[开团]`
  - 公示期卡片：显示倒计时 + `[提出异议]` 按钮
- **规则集市**：类似结构，卡片显示规则包名称、作者、类型、价格；已拥有显示“已在库中”

#### 2.2.2 我的
- **二级Tab**：模组 / 规则 / 人物卡 / 我创作的（仅创作者可见）
- 展示用户已购买、收藏、自创的内容
- 每个资产卡片操作：`[开团]`（已拥有资产）、`[编辑]`（自创草稿）、`[删除]`等

> **边界澄清**：用户购买模组后，在“发现”中该卡片会变为“已在库中”状态，点击 `[开团]` 可直接使用，无需去“我的”页。同时，“我的-模组”中也会出现该模组，作为永久资产列表。

---

### 2.3 我的团

### 2.3 我的团

**定位**：用户管理自己**参与或正在观看**的团。

> **⚠️ 关键约束**：
> 1. 平台**不设独立的“房间大厅”或“公开房间列表”**。
> 2. 所有公开招募性质的团，均通过**社区-招募板**发布招募帖进行。
> 3. **玩家加入房间的路径**：
>    - 申请招募帖 → GM 确认 → 房间出现在“我的房间”列表
>    - 接受邀请 → 房间出现在“我的房间”列表
>    - 直接创建私密团 → 房间出现在“我的房间”列表
> ...
> 4. **OB 观看的路径**：
>    - 在招募板筛选“可观战” → 点击 `[观看团]` → 以 OB 身份进入该团的剧情场（只读）
>    - OB 进入后，该房间**临时出现**在“我的房间”列表中，退出后消失
>    - OB 身份**不可发言、不可掷骰**，仅观看剧情场
>    - OB **可以进入公共场**（默认大厅和被邀请的公共场），在公共场中可以发言（使用平台昵称）

**内容区**：列表 —— **我的房间**


**我的房间列表**：
- 按状态分组：进行中 / 待开始 / 已结束
- 每个房间卡片：房间名、当前场景、规则标签、模组名称、GM昵称、成员头像、状态标识、操作按钮（`[进入]` / `[查看回顾]`）
- 若房间开启了OB，卡片上显示小眼睛图标 （仅标识，点击进入后自动为观战模式）
- **空状态**：显示SVG占位图 + “暂无房间” + 两个按钮：`[去招募板找团]`、`[创建团]`
**顶部操作栏**：
- `+ 创建团` 按钮：点击后进入创建房间页面。
- `加入团` 输入框：输入房间码/邀请链接加入已有房间。

**创建房间页面**：
- **必填**：房间名、规则包（下拉选择）。**规则包一经选定不可更改**。
- **可选**：模组（下拉选择：“无模组” / 从资产库选择已有模组）。
- **初始场景**：若绑定了模组，可预览并调整将导入的场景；若未绑模组，需手动创建第一个剧情场。
- **发布选项**：
  - ☐ 发布到招募板（勾选后展开招募信息表单）
- **点击【创建】后**：
  1. 房间立即创建成功，获得房间码。
  2. **若勾选了“发布到招募板”**：
     - 系统自动在招募板生成一条关联此房间的招募帖。
     - 招募帖的规则包、模组等信息自动继承自房间配置。
     - 招募帖发布后，其他用户可在“社区-招募板”的“找团玩”Tab 中看到并申请加入。
  3. 房间出现在创建者的“我的团-我的房间”列表中。
  4. 所有公开招募性质的团，均通过招募帖进行，**平台不设独立的“房间大厅”或“公开房间列表”**。

**发布到招募板（展开表单）**：
- 招募标题、目标人数、时间安排、难度、开团频率、总时长、可观战、含秘密HO 等（与招募帖表单一致，规则包和模组自动继承）。
---

### 2.4 社区-招募板（双向组车市场）

**定位**：GM招募与玩家求组的**平级、双向**市场。

**页面结构**：一级Tab：`找团玩` / `当KP` / `我的`

#### 2.4.1 找团玩（GM招募帖列表）

- **列表形式**：卡片式，每个卡片包含：
  - 标题、GM头像+昵称、规则标签、题材标签
  - 人数进度（3/5）、时间、招募状态（招募中/即将满员/已满）
  - 可观战标识 👁️（如果房间开启OB）
  - 操作按钮：`[申请加入]` 或 `[观看直播]`（仅当房间已开团且开启OB时显示）
- **空状态**：显示“暂无招募，去[当KP]看看？”或引导“发布第一个招募帖”

#### 2.4.2 当KP（玩家求组帖列表）
- **列表形式**：卡片式，每个卡片包含：
  - 玩家头像+昵称、期望规则/题材、可参与时间、自我介绍（摘要）
  - 玩家自评等级标签（新手/有经验/老手）
  - 操作按钮：`[邀请入团]`（仅GM可见）、`[私信联系]`（所有人可见）
- **空状态**：显示“暂无求组，去[找团玩]看看？”或引导“发布第一个求组帖”

#### 筛选栏（共性与规则无关）
**1. 通用信息（所有规则包共有，支持筛选）**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| 标题 | 文本 | 是 | 简明扼要 |
| 规则包 | 下拉 | 是 | 从平台已发布规则包中选择 |
| 招募类型 | 单选 | 是 | 找玩家 / 找KP / 找测试者 |
| 人数 | 数字（当前/最大） | 是 | 如“2/5” |
| 时间安排 | 文本+选项 | 是 | 单次时间 + 周期（每周几/人齐后定） |
| 难度 | 单选 | 是 | 新手友好 / 普通 / 硬核 |
| 开团频率 | 下拉 | 是 | 每周1次 / 每周2-3次 / 人齐后定 |
| 总时长 | 下拉 | 是 | 短团(<10h) / 中团(10-30h) / 长团(>30h) |
| 可观战 | 复选框 | 否 | 默认关闭 |
| 含秘密HO | 复选框 | 否 | 默认关闭 |
| **模组状态** | **单选** | **是** | **○ 已选定模组（展开资产库选择器） / ○ 待定，人齐后商议 / ○ 无模组，即兴开团** |

> **注意**：选择“已选定模组”时，下方出现资产库选择器，从已拥有的模组中选择一个。招募帖详情页将展示选定的模组信息。

> **设计说明**：只有所有规则包都有的属性才做筛选。规则包专属要求（如COC的信誉限制、DND的等级范围）不在筛选项中，仅在帖子详情中展示。

---

#### 发帖入口（发布招募 / 发布求组）

页面右上角固定按钮：`+ 发布招募`（GM）和 `+ 发布求组`（玩家）。  
点击后弹出模态框，表单分为两个区域：

**1. 通用信息（所有规则包共有，支持筛选）**

| 字段       | 类型        | 必填    | 说明                                                 |
| -------- | --------- | ----- | -------------------------------------------------- |
| 标题       | 文本        | 是     | 简明扼要                                               |
| 规则包      | 下拉        | 是     | 从平台已发布规则包中选择                                       |
| 招募类型     | 单选        | 是     | 找玩家 / 找KP / 找测试者                                   |
| 人数       | 数字（当前/最大） | 是     | 如“2/5”                                             |
| 时间安排     | 文本+选项     | 是     | 单次时间 + 周期（每周几/人齐后定）                                |
| 难度       | 单选        | 是     | 新手友好 / 普通 / 硬核                                     |
| 开团频率     | 下拉        | 是     | 每周1次 / 每周2-3次 / 人齐后定                               |
| 总时长      | 下拉        | 是     | 短团(<10h) / 中团(10-30h) / 长团(>30h)                   |
| 可观战      | 复选框       | 否     | 默认关闭                                               |
| 含秘密HO    | 复选框       | 否     | 默认关闭                                               |
| 规则包      | 下拉        | 是     | 从平台已发布规则包中选择                                       |
| **模组状态** | **单选**    | **是** | **选项：○ 已选定模组（展开资产库选择器） / ○ 待定，人齐后商议 / ○ 无模组，即兴开团** |

**注意**：选择“已选定模组”时，下方出现资产库选择器，从已拥有的模组中选择一个。招募帖详情页将展示选定的模组信息。

**2. 规则专属要求（不参与筛选，仅作描述）**

- 如果选择的是**平台内置主流规则包**（如 COC 7th、D&D 5e），表单会动态显示该规则包常用的结构化字段（例如 COC 显示“信誉限制、年龄/教育、是否混孤”；D&D 显示“等级范围、阵营限制、扩展规则”），GM 按需填写。
- 如果选择的是**其他规则包**（含自定义），则只显示一个多行文本框：“车卡要求及其他说明”，GM 自由填写文本。

> **设计原则**：筛选只针对所有规则包共有的属性；规则包特有的细节不做筛选，避免筛选器臃肿且无意义。规则包专属字段由规则包作者（或平台预置）定义，仅在发帖表单中展示，方便 GM 表达具体要求。
#### 2.4.3 我的
- 展示用户发布的招募帖和求组帖（分开列表或混合）
- 展示用户的申请记录（申请加入的招募帖、收到的申请）
- 支持编辑、关闭、删除自己的帖子

#### 2.4.4 发帖入口
- 页面右上角固定按钮：`+ 发布招募`（GM）和 `+ 发布求组`（玩家）
- 点击后弹出模态框填写对应表单
在“通用信息”中明确：

| 字段  | 类型  | 必填    | 说明                                 |
| --- | --- | ----- | ---------------------------------- |
| 规则包 | 下拉  | **是** | 从平台规则包中选择                          |
| 模组  | 下拉  | 否     | 可选择：“无（即兴团）” / “从资产库选择已有模组” / “待定” |
#### 2.4.5 成团流程

**触发条件**：招募帖状态为“招募中”，且 GM 点击【成团】按钮（人数已满或 GM 手动触发）。

**流程**：
1. 系统检查该招募帖是否已关联 `campaign`（即是否来自路径 B 先建团后发帖）。
   - **若已关联**：直接将该房间的成员列表更新为所有通过申请的玩家，并推送通知。
   - **若未关联**（路径 C：先发帖后成团）：
     - **a. 模组确认**：
       - 若发帖时模组状态为“已选定”或“无模组”，直接使用。
       - 若发帖时模组状态为“待定”，弹出模态框：
	       - 成团 - 选择模组  
		○ 从资产库选择已有模组 [下拉▼]  
		○ 创建一个空白模组（即兴团）  
			[取消] [确认成团]

- **b. 创建房间**：系统根据招募帖的规则包和选定的模组状态自动创建房间（私密）。
- **c. 导入内容**：若绑定了模组，自动导入场景、NPC、线索等。
2. 系统通过站内通知和私信向所有通过申请的成员发送房间码。
3. 房间自动出现在所有成员“我的团-我的房间”列表中。
4. 招募帖状态变为“已关闭”。

**边界情况**：
- 若成团时 GM 取消模组选择，则流程中止，招募帖保持原状态。
- 若成团时模组选择“创建一个空白模组”，则创建的房间无预设内容，GM 需在房间内手动添加。











#### 2.4.6 邀请入团流程（新增完整设计）
- **触发**：GM在“当KP”Tab的求组帖卡片上点击 `[邀请入团]`
**界面元素**：
- 标题："邀请玩家「[玩家昵称]」加入你的游戏"
- 单选框：
  - ○ 邀请至现有房间
  - ○ 为此玩家新建一个房间
- 下拉选择器（当选择"现有房间"时显示）：列出 GM 所有进行中的房间
- 模组选择（当选择"新建房间"时显示）：默认使用玩家期望规则，可切换
- 按钮：【取消】【发送邀请】

**交互规则**：
- GM 在"当KP"列表的求组帖卡片上点击 `[邀请入团]` 时触发此模态框。
- 默认选中"邀请至现有房间"，下拉框显示 GM 进行中的房间列表。
- 选择"为此玩家新建一个房间"时，下方显示模组选择器，默认使用玩家在求组帖中填写的期望规则。
- 点击【发送邀请】后，系统创建邀请记录，并向玩家推送通知。
- 玩家在通知中点击"接受"后，根据 GM 的选择加入现有房间或进入新建的房间。

**数据来源**：
- 玩家昵称：从求组帖中获取。
- 现有房间列表：从 `campaigns` 表中查询 `gm_user_id = 当前GM` 且 `status = 'running' or 'preparing'` 的记录。
- 模组选择器：从 GM 的资产库中查询已拥有的模组。




- **流程**：
  1. GM选择选项后点击“发送邀请”。
  2. 系统创建邀请记录，并向玩家推送通知：“GM XXX 邀请你加入团《模组名称》”。
  3. 玩家点击通知或进入“我的-通知”页面，看到邀请卡片，点击“接受”或“拒绝”。
  4. 接受后：
     - 如果GM选择现有房间，玩家被加入该房间（角色卡需GM分配或玩家自选）。
     - 如果GM选择新建房间，系统自动创建私密房间（默认使用GM选择的模组，或玩家期望规则下的默认模组），并将两人加入房间。
  5. 房间自动出现在双方“房间大厅-我的房间”列表中。
- **UX收益**：从看到求组帖到一起玩，只需GM 2次点击 + 玩家1次点击，无页面跳转。

---


## 2.5 个人（个人中心）

**定位**：用户个人资料、社交关系、通知、账号设置的统一管理中心。所有资产相关内容（模组、规则、角色卡）已移至“资产库”，本页面聚焦身份和社交。

**布局**：左侧导航 + 右侧内容区（与资产库保持一致风格）

### 2.5.1 左侧导航（宽 200px）

- **个人主页编辑**（入口，可预览公开页）
- **通知**
- **私信**
- **关注/粉丝**
- **账号设置**
- **创作者后台**（仅创作者身份可见，点击跳转完整后台页面）

### 2.5.2 右侧内容区（默认展示“个人主页编辑”）

#### 个人主页编辑

- **头像**：支持上传图片（≤2MB），裁切为 1:1 圆形
- **昵称**：可修改，2~16 字符，不包含敏感词
- **简介**：100 字以内，支持纯文本
- **身份标签**（多选）：
  - 玩家（默认）
  - GM
  - 创作者（需审核通过后自动显示，不可自选）
- **隐私设置**：
  - 谁可以看到我的高光墙：所有人 / 仅关注者 / 仅自己
  - 谁可以给我发私信：所有人 / 仅关注者 / 仅互关
- **预览公开主页**按钮：点击新窗口打开 `/u/{uid}`

#### 通知中心

**Tab**：`全部` / `系统通知` / `交易通知` / `社交通知` / `审核通知`

**通知类型与样式**：

| 类型 | 示例 | 操作按钮 |
|------|------|----------|
| 系统 | “您的房间【常暗之厢】已创建成功” | 无 |
| 交易 | “您购买的模组【死光】已入库” | [查看模组] |
| 社交 | “GM 守秘人小李 邀请你加入团《常暗之厢》” | [接受] [拒绝] |
| 审核 | “您发布的模组【深渊之底】已通过公示期” | [查看模组] |

**邀请通知特殊处理**（对应 2.4.6 节）：
- 卡片形式，包含邀请人、团名、模组名称
- 点击 [接受] 后：
  - 若邀请至现有房间：直接加入，房间出现在“房间大厅-我的房间”
  - 若邀请至新建房间：系统自动创建房间并加入，双方房间列表更新
- 接受后通知状态变为“已处理”，不可再次操作

**交互**：
- 支持批量标记已读
- 支持按类型筛选
- 支持删除单条通知

#### 私信

**布局**：左侧会话列表 + 右侧聊天区（类似微信）

- **会话列表**：显示最近联系人（头像、昵称、最后一条消息预览、时间）
- **聊天区**：
  - 支持文字、图片（上传至 OSS）
  - 支持引用回复
  - 支持举报消息
- **新私信入口**：点击“新建会话”，搜索用户 UID 或昵称

#### 关注/粉丝

- **Tab**：`我的关注` / `我的粉丝`
- **列表形式**：每个用户卡片包含头像、昵称、简介、互关标识
- **操作**：关注 / 取消关注 / 移除粉丝
- **跳转**：点击卡片进入对方个人主页

#### 账号设置

**分组表单**：

1. **账号安全**
   - 手机号（已绑定，不可修改，可更换）
   - 密码（可选设置，留空表示仅验证码登录）
   - 登录设备管理（显示最近登录设备，可踢出）

2. **内容偏好**
   - 偏好的规则（多选：COC 7th, D&D 5E, Pathfinder 等）
   - 偏好的题材（多选：恐怖、奇幻、科幻、悬疑等）
   - 推荐算法开关（关闭后推荐只基于热门，不个性化）

3. **主题**
   - 浅色 / 深色 / 跟随系统

4. **数据管理**
   - 导出我的数据（ZIP 包：角色卡、私信、通知、跑团日志摘要）
   - 删除账号（15 天冷静期，期间可撤销）

#### 创作者后台（仅创作者身份可见）

**入口**：左侧导航栏“创作者后台”按钮（非 Tab，点击跳转独立页面）

**独立页面结构**（简化版，完整版在 PC 端新窗口打开）：

- **数据概览卡片**：
  - 本月收入（元）
  - 总销量（模组+规则）
  - 作品总数
  - 创作者等级（Lv1~Lv5）
- **订单管理**：表格展示最近订单（商品名、金额、买家、时间、状态）
- **提现申请**：表单（提现金额、收款账户），需满足最低 100 元
- **评价列表**：展示收到的用户评价（星级+评语）
- **作品管理**：快速跳转至“资产库-我的-我创作的”

> **移动端**：创作者后台仅展示数据概览卡片和作品管理入口，详细订单和提现引导至 PC 端。

---

### 2.5.3 空状态与引导

| 场景 | 显示内容 |
|------|----------|
| 无任何通知 | SVG 占位图 + “暂无通知，去看看招募板” + [去社区] 按钮 |
| 无私信会话 | “暂无私信，给好友发一条吧” + [查找用户] 按钮 |
| 无关注/粉丝 | “还没有关注任何人，去社区发现有趣的玩家” + [去社区] 按钮 |
| 非创作者点击“创作者后台” | 提示“你尚未成为创作者，[去申请]”，跳转创作者申请表单 |

---

### 2.5.4 与“资产库”的边界说明

| 功能 | 归属 | 原因 |
|------|------|------|
| 模组/规则/角色卡管理 | 资产库 | 属于“我的数字资产” |
| 我创作的作品编辑 | 资产库-我的-我创作的 | 属于创作工作区 |
| 个人资料、通知、私信、设置 | 我的 | 属于身份和社交 |
| 创作者后台（收入、订单） | 我的 | 属于个人中心的管理功能 |

## 2.6 跑团房间

> **简化开团流程说明**：
> 当用户从首页“快速组车卡片”或资产库“已拥有模组/规则包”点击 `[开团]` 时，系统将：
> 1. 自动创建一个绑定该规则包/模组的私密团。
> 2. 跳过房间名填写、招募选项等步骤，直接进入房间。
> 3. 房间默认名为“{模组名} 的团”，GM 可后续在房间设置中修改。
> 此流程旨在将“从拥有资产到开始游戏”的点击次数降至最低。

> **⚠️ 进入房间的路径**：
> 4. **从招募板成团**：玩家申请招募帖，GM 确认后，房间出现在双方的“我的团-我的房间”列表中。
> 5. **直接创建私密团**：GM 创建房间时不发布到招募板，通过分享房间码邀请成员加入。
> 6. **接受邀请**：玩家接受 GM 的邀请后，房间出现在“我的团-我的房间”列表中。
> 7. **OB 观看**：在招募板点击“可观战”房间的 `[观看团]` 按钮，以只读身份进入。房间临时出现在 OB 的“我的房间”列表中，退出后消失。

### 2.6.1 整体布局（**网页版宽屏布局**）

采用**两栏+浮动面板**布局：左侧场景导航（240px，可折叠）、中间聊天区（弹性）。
助理台默认隐藏，点击顶部栏「助理台」按钮从**右侧滑出**，覆盖在聊天区之上（宽度 320px），不挤压主内容区。移动端改为底部抽屉（从下往上滑出）。

```
┌─────────────────────────────────────────────────────────────────────────────────┐  
│ [←] 黑暗边缘团 │ 全局剧情时间：第1日 08:30 │ [+30m] [1h] │ [GM] [≡] │  
│ │ 在场角色：张三、李四、王五 │ │ │  
├───────────┬─────────────────────────────────────────────┬───────────────────────┤  
│ 当前场景 │ [剧情场] [私密场] [公共场] │ 助理台 │  
│ 波士顿图书馆│ ┌─────────────────────────────────────────┐ │ ┌───────────────────┐ │  
│ 第1日 08:30│ │ 张三：我要调查书架... │ │ │ 角色卡 │ │  
│ │ │ [骰] 侦查检定：1d100=35/60 成功 │ │ │ 调查员张三 │ │  
├───────────┤ │ 李四：我去询问馆长... │ │ │ HP 12/12 │ │  
│ 轨迹矩阵 │ │ 系统：张三 离开酒馆，前往图书馆（预计08:45）│ │ │ 技能：侦查60 │ │  
│ 精简版 │ │ ... │ │ ├───────────────────┤ │  
│ 14:00 酒馆 │ └─────────────────────────────────────────┘ │ │ 指令速查 │ │  
│ 15:00 警局 │ [输入框] [发送] │ │ .ra 侦查 │ │  
│ 15:30←图书馆│ │ │ .r 1d100 │ │  
├───────────┤ │ │ [申请移动] │ │  
│ 在场角色 │ │ ├───────────────────┤ │  
│ 张三● │ │ │ 投骰历史 │ │  
│ 李四● │ │ │ 今日: 3次 │ │  
│ 王五○ │ │ ├───────────────────┤ │  
│ │ │ │ 暗骰记录(仅GM) │ │  
│ │ │ │ [GM广播] 按钮 │ │  
└───────────┴─────────────────────────────────────────────┴───────────────────────┘

text┘
```


**顶部栏**：
- 房间名称、返回按钮
- **全局剧情时间**（所有成员可见），格式 `第N日 HH:MM`
- GM 可见时间推进按钮：`+30分钟`、`+1小时`，点击后弹出确认框
- `[GM]` 按钮：打开 GM 控制台折叠面板（仅 GM 可见）
- `[≡]` 菜单：收起/展开左侧导航

**左侧导航栏**（宽度 240px）：
- **当前场景卡片**：显示当前激活的剧情场名称、全局时间、场景氛围简述
- **轨迹矩阵精简版**：显示最近 2-3 个时间节点，当前节点高亮，未来节点虚线框
- **在场角色列表**：显示当前剧情场内的所有角色，GM 可右键操作
- **场景切换**：点击其他剧情场，中间聊天区立即切换

**中间聊天区**：
- **顶部 Tab**：剧情场 / 私密场 / 公共场（横向滚动）
  - 公共场Tab下显示所有公共场列表（默认公共场 + 新建的公共场）
  - 默认公共场标识为“大厅”，新建公共场显示自定义名称
- **消息流**：按剧情时间 + 消息 ID 排序（公共场无剧情时间，按真实时间倒序）
- **输入区**：文本输入框 + 快捷指令按钮

**右侧助理台**（宽度 320px，可折叠）：
- **角色卡区域**：显示当前绑定角色摘要（头像、姓名、HP条），点击展开完整编辑模态框。未绑定时显示“未绑定角色卡”，提供创建/选择入口。
- **指令速查**：平台通用指令 + 当前规则包支持的指令，每条指令带 `[使用]` 按钮。**含 `[申请移动]` 快捷入口**。
- **投骰历史**：当前用户的掷骰记录，支持按技能名、指令类型、结果筛选。
- **暗骰记录**（仅 GM）：GM 执行的暗骰结果列表，带锁图标。
- **快速广播**（仅 GM）：输入框 + 发送按钮，一键广播到所有场（剧情场+私密场+公共场），自动添加 `[GM公告]` 前缀，防刷屏（1分钟1次）。

> **移动端适配**：右侧助理台移除，功能移至底部快捷栏（详见附录 J）。


**房间内编辑角色卡注意事项**：

- 当 GM 或玩家在房间内修改角色的基础属性（如力量、体质）或技能值时，修改**仅影响当前团的 Instance**，不会同步到角色卡模板。
- 编辑模态框底部应显示轻提示文案：“ 修改仅在本团生效，不影响角色卡模板”。
- 若需永久修改，请引导用户前往「我的 → 人物卡管理」编辑 Template。
---

### 2.6.2 时间控制与预约移动

#### 2.6.2.1全局剧情时间显示

- **所有成员**：顶部栏显示当前全局剧情时间（只读）
- **GM 特权**：可点击推进按钮或点击时间数字打开自定义时间选择器（滚轮，精确到分钟）
#### 2.6.2.2时间推进确认弹窗（GM）

**触发**：GM 点击时间推进按钮（+30分钟、+1小时）或自定义时间。

**弹窗内容**：
- 提示文本：“推进全局时间至 第X日 HH:MM”
- 列出将触发执行的所有预约移动（角色、起点、终点、预计到达时间）。
- 若某移动的目标场景已被删除，或目标场景类型不是剧情场，该项显示红色警告，并注明“将被自动取消”。
- 按钮：【取消】【确认推进】

**确认后行为**：服务端在事务中执行时间推进、更新角色位置、记录位置历史，并通过 Socket.IO 广播结果。

#### 2.6.2.3玩家申请移动

**入口**：
- 助理台指令速查中的 `[申请移动]` 按钮
- 或输入 `/move` 指令
**界面元素**：
- 标题："申请移动"
- 下拉选择器：目标场景（列出当前团所有剧情场）
- 交通方式下拉（仅当团启用场景连通时显示）：步行 / 骑行 / 驾车
- 预计耗时（自动计算，只读）
- 预计到达时间（自动计算，只读）
- 按钮：【取消】【提交申请】

**交互规则**：
- 入口：助理台指令速查中的 `[申请移动]` 按钮，或输入 `/move` 指令。
- 选择目标场景后，若启用场景连通且选择了交通方式，系统根据连通表自动计算预计耗时和到达时间。
- 若未启用场景连通，则只显示目标场景选择，不显示交通方式和耗时。
- 点击【提交申请】后，生成一条 `status = 'pending'` 的预约移动记录，并向 GM 发送待审批通知。

**数据来源**：
- 目标场景列表：从 `scenes` 表中查询当前团的 `type = 'spatial'` 的记录。
- 耗时计算：从 `scene_connections` 表中查询最短路径并累加耗时。

- 若未启用场景连通（即团创建时未勾选“启用场景连通与自动移动调度”），则只显示目标场景选择，不显示交通方式和预计耗时。GM 审批时需手动填写到达时间。
- 提交后，GM 控制台的“待审批移动”列表中出现新申请，玩家收到“申请已提交”提示。

#### 2.6.2.4GM 审批面板

**入口**：GM 控制台 → “待审批移动”标签页。
**界面元素**：
- 标题："待审批移动"
- 表格列：申请人 | 当前位置 | 目标场景 | 交通方式 | 预计耗时 | 申请时间 | 操作
- 操作按钮：【批准】【拒绝】【修改时间】（每个申请一行）

**交互规则**：
- 入口：GM 控制台 → "待审批移动"标签页。
- 点击【批准】：弹出时间修改框（预设到达时间 = 当前全局时间 + 预计耗时），GM 可调整。确认后预约移动状态变为 `approved`，并通知玩家。
- 点击【拒绝】：预约移动状态变为 `cancelled`，通知玩家申请被拒绝（可选填理由）。
- 点击【修改时间】：仅修改到达时间，状态保持 `pending`，并通知玩家时间已调整。
- 若目标场景已被删除，该行显示红色警告，且【批准】按钮禁用。

**数据来源**：
- 待审批列表：从 `scheduled_moves` 表中查询 `status = 'pending'` 且 `campaign_id = 当前团` 的记录。
- 角色信息：关联 `character_sheets` 表获取角色名。
- 场景信息：关联 `scenes` 表获取场景名。

- **批准**：弹出时间修改框（预设到达时间 = 当前全局时间 + 耗时），GM 可调整。确认后生成预约移动记录，状态变为 `approved`，系统通知玩家。
- **拒绝**：通知玩家申请被拒绝，可填写理由（可选）。
- **修改时间**：仅修改到达时间，不改变其他。

#### 2.6.2.5强制移动（GM 特权）

**入口**：
- 左侧导航在场角色列表中，右键角色 → “强制移动”
- 轨迹矩阵单元格右键 → “移动此角色到此场”

**弹窗**：

```
┌─────────────────────────────────────────────────────────┐
│  强制移动 张三                                          │
│  目标场景：[下拉选择剧情场 ▼]                           │
│  到达时间：[立即] / [设定剧情时间：第1日 09:00]         │
│  [取消]                                [确认强制移动]   │
└─────────────────────────────────────────────────────────┘
```

- 立即执行：取消该角色所有未执行预约，直接更新位置和个人时间为当前全局时间。
- 定时执行：创建一个已批准的预约移动（跳过玩家申请环节），到期自动执行。
- 聊天区插入 `[GM强制]` 标记的系统消息。

#### 2.6.2.6预约移动执行（系统自动）

当 GM 推进全局时间时，系统自动检查所有 `status='approved'` 且 `execute_at_story <= 新时间` 的预约移动，按时间升序依次执行：

1. **离开原场景**：更新 `campaign_character_instances.current_spatial_scene_id = NULL`，位置历史中旧记录的 `story_time_left` 设为离开时间。
2. **在途状态**：角色不显示在任何场景，轨迹矩阵中对应时间段留白。
3. **到达目标场景**：更新 `current_spatial_scene_id = 目标场景`，个人剧情时间 = 到达时间，位置历史插入新记录（`story_time_entered = 到达时间`）。
4. **广播**：通过 Socket.IO 推送 `position_changed` 事件，所有客户端实时更新界面。

---
#### 2.6.2.7 技能同步提示组件
**触发场景**：
1. **检定时检测到跨团技能成长**：玩家发起技能检定（如 `ra 侦查`）时，系统对比 Template 与当前 Instance 的技能值，若 Template 更高，则弹出确认框。
2. **进入房间时检测到跨团技能成长**：玩家进入房间时，后台检测到该角色卡在其他团中有技能成长，顶部显示可关闭横幅。
**弹窗样式**：

┌─────────────────────────────────────────────────┐  
│ 🔄 检测到技能成长 │  
├─────────────────────────────────────────────────┤  
│ 你在其他团中的「侦查」技能已提升至 55。 │  
│ 当前团的技能值为 50。是否立即同步？ │  
│ │  
│ [暂不同步] [立即同步] │  
└─────────────────────────────────────────────────┘

text

- 点击「立即同步」：Instance 技能值更新为 Template 值，弹窗关闭，检定继续使用新值。
- 点击「暂不同步」：保留 Instance 旧值，本次团内不再提示该技能的同步，但 GM 可手动触发。
**顶部横幅样式**：

┌──────────────────────────────────────────────────────────────────┐  
│ ℹ️ 你的角色卡在其他团中有技能成长（侦查 50→55，图书馆 40→45） [同步] [忽略] ✕ │  
└──────────────────────────────────────────────────────────────────┘

text

- 背景色：`--color-info-bg`，文字色：`--color-info`。
- 点击「同步」：批量同步所有有成长的技能，横幅消失。
- 点击「忽略」或「✕」：关闭横幅，本次登录会话内不再显示。
**交互规则**：
- 若技能值差距 ≥5 点，不弹窗，直接强制同步并显示轻提示：“技能「侦查」已自动同步至 55”。
### 2.6.3 轨迹矩阵（会员功能，需开团时启用）

**界面元素**：
- 横向滚动表格：
  - 纵轴（行）：时间切片（每 15 或 30 分钟，可调节粒度）。
  - 横轴（列）：剧情场列表（按创建顺序排列）。
- 单元格内容：
  - 灰色单元格：已发生的时间点，显示角色头像（多个可平铺或折叠）。
  - 红色竖线：标记当前全局剧情时间所在的列。
  - 白色单元格 + 虚线框：未来时间点，显示已批准的预约移动预览。
- 右键菜单：移动此角色到此场 / 查看角色状态快照。

**交互规则**：
- 入口：GM 控制台 → "轨迹矩阵"标签页（仅当团创建时启用此模块才显示）。
- 点击单元格 → 弹出该时刻该场的角色状态快照（角色卡摘要）。
- 右键单元格 → 弹出菜单，可触发强制移动或查看快照。
- 支持选择时间范围导出剧情回顾（CSV/JSON）。
- 拖拽预约移动的虚线框可调整到达时间（需重新审批）。
- **只读约束**：GM 不可直接编辑单元格内容。

**数据来源**：
- 时间切片：根据 `campaigns.global_story_time` 和配置的粒度生成。
- 场景列表：从 `scenes` 表中查询当前团的 `type = 'spatial'` 的记录。
- 历史位置：从 `position_history` 表中查询，按 `story_time_entered` 和 `story_time_left` 确定每个角色在特定时间切片的位置。
- 预约移动预览：从 `scheduled_moves` 表中查询 `status = 'approved'` 且 `execute_at_story > 当前时间` 的记录。
#### 移动端适配（时间轴视图）

**默认视图**：垂直时间轴，按时间倒序排列，每条节点显示：
- 时间（如“第1日 15:30”）
- 该时刻发生的场景切换事件摘要（如“张三 进入 图书馆”）
- 可点击展开查看该时刻所有场景的角色位置快照（卡片列表）

**场景筛选**：顶部下拉选择特定场景，时间轴只显示该场景的角色位置变化。

**GM 操作**：长按时间节点弹出动作菜单（强制移动、查看快照）。

> 技术实现：使用虚拟滚动列表渲染时间轴，快照数据按需加载。

---

### 2.6.4 场景连通配置（高阶 GM 功能，会员专享）

> 需在团创建时于「高级设置」中勾选“启用场景连通与自动移动调度”。（**会员专享**）


**移动端**：仅支持只读查看路径列表，编辑提示“请在电脑上操作”。

---
**界面元素**：
- 标题："场景连通路径配置"
- 按钮：【导入】【导出】【+ 添加连通】
- 表格列：起点 | 终点 | 步行(min) | 骑行(min) | 驾车(min) | 操作
- 操作按钮：【编辑】【删除】（每行）

**交互规则**：
- 入口：GM 控制台 → 场景管理 → "路径编辑"（需团创建时启用场景连通）。
- 点击【+ 添加连通】：弹出表单，选择起点、终点，填写各交通方式耗时（分钟），支持设为双向。
- 点击【编辑】：修改已有连通的耗时。
- 点击【删除】：移除该连通关系。
- 【导入/导出】：支持上传/下载 JSON 配置文件，便于复用。

**数据来源**：
- 连通列表：从 `scene_connections` 表中查询 `campaign_id = 当前团` 的记录。
- 起点/终点下拉选项：从 `scenes` 表中查询当前团的 `type = 'spatial'` 的记录。
- 默认耗时：若某交通方式不可用，设为 `null` 或留空。
### 2.6.5 消息与线索卡片

#### 消息类型视觉规范

| 类型    | 样式               | 示例                           |
| ----- | ---------------- | ---------------------------- |
| 普通叙事  | 白色气泡，左对齐，角色头像+名称 | “张三：我推开门...”                 |
| 掷骰结果  | 浅灰色卡片，等宽字体，带骰子图标 | `侦查检定：1d100 = 35/60 成功`      |
| 系统消息  | 居中灰色小字，带时间戳      | `[系统] 张三 进入 图书馆`             |
| GM 公告 | 黄色背景，`[GM公告]` 前缀 | `[GM公告] 所有人注意，时间推进30分钟`      |
| 线索卡片  | 带主题图标，可点击展开      | `染血的纸条`                      |
| 移动事件  | 斜体浅灰色，带箭头图标      | `→ 张三 离开酒馆，前往图书馆（预计08:45到达）` |

#### 线索卡片展开

点击线索卡片后，弹出模态框，按 GM 设置的呈现样式（如“血字告白”、“古籍密卷”）展示完整内容，支持富文本和艺术化主题。

### 新增系统截断消息示例

|类型|样式|示例|
|---|---|---|
|普通叙事|白色气泡，左对齐，角色头像+名称|“张三：我推开门...”|
|掷骰结果|浅灰色卡片，等宽字体，带骰子图标|`侦查检定：1d100 = 35/60 成功`|
|系统消息|居中灰色小字，带时间戳|`[系统] 张三 进入 图书馆`|
|**派生值截断**|**居中灰色小字，带警告图标**|`[系统] 由于体质变化，张三的HP从15调整为12`|
|GM 公告|黄色背景，`[GM公告]` 前缀|`[GM公告] 所有人注意，时间推进30分钟`|
|线索卡片|带主题图标，可点击展开|`染血的纸条`|
|移动事件|斜体浅灰色，带箭头图标|`→ 张三 离开酒馆，前往图书馆（预计08:45到达）`|

---

### 2.6.6 GM 控制台（折叠面板）

**位置**：顶部栏下方，默认收起（仅显示细横条）。点击 `[GM]` 按钮向下滑出。

**标签页**（横向滚动）：

1. **时间控制**：
   - 显示当前全局时间
   - 推进按钮（+30分钟、+1小时、自定义）
   - 预约移动预览列表（未来将执行的移动）

2. **待审批移动**：
   - 列表显示所有 `pending` 状态的移动申请，支持批准/拒绝/修改时间

3. **场景管理**：
   - 快速添加剧情场
   - 强制移动玩家（入口）
   - **路径编辑**（若启用场景连通）

4. **NPC 控制**：
   - 可扮演 NPC 列表，支持快速扮演
   - 创建临时 NPC

5. **线索库**：
   - 显示模组线索 + GM 临时线索
   - 设置线索呈现样式（血字告白、古籍密卷等）
   - 发放线索（选择接收者）

6. **高级**（**会员专享**）：
   - 轨迹矩阵入口（仅当团创建时启用该模块显示）
   - 网格地图入口（仅当团创建时启用该模块显示）

**角色死亡状态（V1.0 预留）**：

- 当角色 Instance 的 `status` 变为 `dead` 时：
  - 左侧导航「在场角色列表」中，该角色头像置灰，并显示红色「✕」标记。
  - 角色卡面板顶部显示红色横幅：“该角色已死亡，无法进行操作”。
  - 所有操作按钮（扣血、编辑、检定等）置灰禁用。
  - GM 仍可查看角色卡，但无法修改。

---

### 2.6.7 跑团房间空状态与引导

| 场景 | 显示内容 |
|------|----------|
| 无任何剧情场 | 中间区域显示：“暂无剧情场，请 GM 在控制台添加” |
| 玩家未绑定角色卡 | 助理台显示：“未绑定角色卡”，并提供“选择已有角色卡”和“创建角色卡”按钮 |
| 无聊天消息 | 聊天区显示 SVG 占位图 + “游戏尚未开始，等待 GM 推进时间” |
| 无可预约移动（无场景连通） | 移动申请模态框中交通方式不可选，只显示目标场景 |

---

### 2.6.8 组件权限与位置汇总

| 组件          | 权限     | 桌面端位置          | 移动端位置       |
| ----------- | ------ | -------------- | ----------- |
| 全局时间显示      | 所有人    | 顶部栏            | 顶部栏         |
| 时间推进按钮      | 仅 GM   | 顶部栏            | 长按顶部时间弹出    |
| 场景导航（剧情场列表） | 所有人    | 左侧栏            | 汉堡菜单抽屉      |
| 轨迹矩阵精简版     | 所有人    | 左侧栏            | 汉堡菜单内       |
| 轨迹矩阵完整版     | GM（会员） | GM控制台 → 高级     | 时间轴视图（只读）   |
| 助理台         | 所有人    | 右侧栏（可折叠）       | 底部抽屉        |
| 申请移动        | 玩家     | 助理台 / 指令       | 底部快捷栏 → 模态框 |
| 审批移动        | 仅 GM   | GM控制台 → 待审批    | 底部动作表单      |
| 强制移动        | 仅 GM   | 右键角色 / 轨迹矩阵单元格 | 长按角色 / 时间节点 |
| 场景连通配置      | GM（会员） | GM控制台 → 场景管理   | 只读，编辑提示 PC  |
| 消息/线索卡片     | 所有人    | 聊天区            | 聊天区（可点击展开）  |
_（注：移动端的具体交互应由《附录 J》统一解释，此处只需保持描述一致即可。）_

---

## 2.7 跑团房间内部组件规范（快速参考）

|组件|权限|桌面端位置|移动端位置|
|---|---|---|---|
|全局时间显示|所有人|顶部栏|顶部栏|
|时间推进按钮|仅 GM|顶部栏|长按时间弹出|
|场景导航|所有人|左侧栏（可折叠）|汉堡菜单|
|轨迹矩阵精简版|所有人|左侧栏|汉堡菜单内|
|助理台|所有人|右侧栏（可折叠）|底部快捷栏|
|移动申请|玩家|助理台 / 指令|底部快捷栏 → 模态框|
|审批移动|仅 GM|GM控制台 → 待审批|底部动作表单|
|强制移动|仅 GM|右键角色 / 轨迹矩阵单元格|长按角色 / 时间节点|
|轨迹矩阵完整版|GM（会员）|GM控制台 → 高级|时间轴视图（只读）|
|场景连通配置|GM（会员）|GM控制台 → 场景管理|只读，编辑提示 PC|

---

### 2.8 冷启动数据填充策略

| 模块 | 冷启动处理 | 负责人 |
|------|------------|--------|
| 首页-为你推荐 | 平台预置10+个精选模组/规则包 | 运营/产品 |
| 首页-组车动态 | 官方GM发布3-5个示例招募帖；运营发布2-3个示例求组帖 | 运营 |
| 首页-快速组车卡片 | 始终显示，无数据依赖 | 前端 |
| 资产库-发现 | 同为你推荐 | 运营 |
| 社区-招募板-找团玩 | 官方GM发布示例招募帖 | 运营 |
| 社区-招募板-当KP | 运营发布示例求组帖，或显示“成为第一个求组的玩家”引导 | 运营 |
| 房间大厅 | 用户无房间时显示空状态引导 | 前端 |
| 新手入门指南 | 平台预置教程内容 | 产品/技术 |

---

## 三、关键交互流程总结

| 用户目标 | 路径 | 点击次数 |
|----------|------|----------|
| 开一个私密团（和朋友玩） | 资产库点击`[开团]` → 填房间名 → 创建 | 2次（含表单确认） |
| 开一个公开招募团 | 资产库点击`[开团]` → 填房间名 + 勾选“公开招募” → 创建 → 跳转招募板 → 补充信息发布 | 4-5次 |
| GM招募玩家 | 社区→招募板→发布招募帖 → 填写表单 → 发布 | 3-4次 |
| 玩家找团 | 社区→招募板→找团玩 → 筛选 → 申请加入 | 3次 |
| GM邀请求组玩家 | 社区→招募板→当KP → 点击`[邀请入团]` → 选择现有/新建房间 → 发送 | 3次 |
| 玩家接受邀请 | 收到通知 → 点击接受 | 2次 |
| 观看一场公开招募团的游戏 | 社区→招募板→找团玩 → 筛选“可观战” → 点击`[观看直播]` | 3-4次 |

---

## 四、与产品设计蓝图的同步更新建议

基于本页面设计方案，产品设计蓝图（V1.1）需要同步修改以下部分：

1. **2.3 跑团房间**：增加“简化开团流程”说明，移除“公开搜索房间”相关描述。
2. **2.5 社区与社交**：招募板部分增加“邀请入团”模态框设计和“可观战”筛选。
3. **4.6 房间大厅**：改为只展示“我的房间”，移除“公开房间列表”和“招募中”Tab。
4. **附录D 页面设计**：替换为本文档V3.0。

---
---

## 附：AI 生成页面指引

本文档中的页面布局描述均为**规则级描述**，不包含具体的 CSS 样式、间距、响应式断点。AI 在生成具体页面时，应遵循以下步骤：

1. **读取本附录**：获取页面核心模块、组件清单、权限矩阵。
2. **读取《附录 B：设计令牌》**：获取颜色、字体、圆角、阴影等视觉变量。
3. **读取《产品设计.md》中对应功能的验收标准**：确保交互逻辑正确。
4. **生成布局**：使用 Slate 色系、遵循 2.1.2 定义的卡片网格列数（桌面4列，移动2列），自动推导字段排列。
5. **状态处理**：根据本附录中的“空状态与引导”生成对应占位图。

AI 生成的内容应接受人工检查，重点验证：
- 权限控制是否正确（GM 独有功能是否对玩家隐藏）。
- 移动端是否应用了《附录 M》的降级方案。
- 表单必填项是否与《产品设计》一致。
## 五、文档变更记录

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| V2.3 | 2026-04-09 | 初始版本（双向组车、冷启动优化） |
| V3.0 | 2026-04-09 | 整合简化开团、移除公开搜索、增加邀请入团模态框、观战融入招募板；删除单人试玩等后期功能 |

---

**文档状态**：定稿，待进入UI设计阶段。  
**下一步**：输出高保真原型（Figma），并进行可用性测试。