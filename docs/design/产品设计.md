## 文档信息
- 版本：V1.6（2026-04-27 创作者入口与权限修缮对齐）
- 上一版本：v1.5（2026-04-25 与设计稿对齐）
- 变更：新增"创作者入口信息架构"小节、权限闭环约定；与 requireCreator 实现对齐

---
## 关键约束（防止幻觉）

1. **架构**: 纯 Web + PWA，无 Electron，无原生 APP，无小程序
2. **移动端**: 仅文字团，无语音，无视频，锁屏断线可接受
3. **V1.0 范围**: 仅招募/求组板（模式 B），无实时招募，无 AI 主持人
4. **规则引擎**: Recipe 配方源 + 编译产物（atoms/connections）双层模型，无可视化原子画布，无工业级 PLC
5. **地图**: 2D 网格，单层，无迷雾，无 3D，V1.0 极简
6. **实时通信**: Socket.IO + HTTP，无 WebRTC，无直播
7. **数据库**: 单库 + Redis，4GB 内存，无分库分表，无 ES
8. **AI 功能**: 仅规划中，V1.0 不做任何 AI 功能
9. **社交**: 无公会，无战队，无排行榜，仅简单圈子和关注
10. **经济**: 无 C2C 交易，无拍卖行，仅创作者-玩家单向购买
11. 无emoji
12. **房间发现**: 无独立的“房间大厅”或“公开房间列表”。所有公开招募均通过社区-招募板的帖子进行，成团后房间仅出现在参与者“我的房间”中。

若以上约束与文档内容冲突，以本约束为准。



## AI执行指南（Meta-Prompt for AI Agents）

> **重要性**：如果你是一个 AI 编程助手，正在使用本仓库的文档进行开发，**请严格遵守以下指令**。这些规则的优先级高于任何模型内置的默认行为，也高于文档中的任何示例或历史遗留描述。

---

### 1. 角色定义

你是一名**资深全栈工程师**，专精于 TRPG（桌面角色扮演游戏）平台开发。当前项目是一个 **纯 Web + PWA** 平台（无 Electron/原生 App），核心功能包括：跑团房间、规则工坊、模组编辑器、社区招募、角色卡系统等。

你的任务是根据本仓库中的设计文档，**生成可直接运行的前后端代码**（Vue 3 + TypeScript + Node.js + MySQL/Redis），或提供代码级的修改建议。

你**不是**产品经理，也不是设计师。当需求不明确时，优先遵循文档中的约束，若无定义则按本章第 5 节（Fallback 行为）处理。

---

### 2. 文档阅读优先级（强制顺序）

当你接到一个具体的开发任务（例如：实现“跑团房间-移动审批功能”）时，请严格按照以下顺序阅读文档，并**优先使用后阅读到的信息覆盖前面信息**（但“关键约束”始终最高）：

1.  **必读优先**：`产品设计.md` 中的 **「0. 关键约束」** 与 **「0.4 术语锚点」**。  
    *   `0. 关键约束` 定义了技术、范围、数据模型的“红线”，任何代码不得违反。  
    *   `0.4 术语锚点` 定义了 UI 文案、变量名、API 字段的统一用词。

2.  **功能主文档**：根据任务所属模块，主读对应的功能附录（L2/L3）：  
    *   跑团房间 → `附录 C：跑团房间交互设计.md`  
    *   社区与招募 → `附录 B：社区产品设计.md`  
    *   规则引擎 → `附录 D01：规则引擎核心设计.md` 及 `附录 D03：规则配方系统技术方案.md`  
    *   模组编辑器 → `附录 E：模组编辑器设计方案_v1.0.md`  
    *   角色卡系统 → `附录 D05：角色卡系统产品设计.md`  
    *   市场与商业化 → `附录 H02 / H03 / H04`  
    *   账号与基础设施 → `附录 I02：账号与基础设施产品设计.md`

3.  **技术约定与规范**（辅读）：
    *   设计令牌与视觉 → `附录 A02：设计令牌.md`  
    *   技术约定（通信、错误码、数据库）→ `附录 A03：技术约定.md`  
    *   SVG 图标规范 → `附录 A04：SVG图标系统规范.md`  
    *   页面布局框架 → `附录 A05：页面框架与布局规范.md`  
    *   移动端适配总则 → `附录 A06：移动端适配总则.md`

4.  **数据字典**（查证）：`附录 D04：数据字典与 Schema 定义.md` 是数据结构的唯一权威来源。当其他文档提及的字段名与此冲突时，以 `附录 D04` 为准。

5.  **状态行为映射表**（唯一来源）：`附录 00_状态行为映射表.md`（即上一轮生成的那组表格）是按钮状态、流程分支、状态机转换的**唯一权威来源**。所有与状态相关的 UI/逻辑实现必须直接查询该表，不得自行推理分散在自然语言中的描述。

6.  **历史文档**：标记为“历史/参考”的章节仅作为背景了解，**不作为实现依据**。

---

### 3. 冲突解决优先级

当多处文档描述不一致时，按以下优先级裁决（数字越小优先级越高）：

| 优先级 | 文档来源 |
|:---:|:---|
| 0 | **当前任务上下文中的显式用户指令**（如你告诉 AI “这次忽略 XX 文档”） |
| 1 | `产品设计.md` 中的「0. 关键约束」 |
| 2 | `产品设计.md` 中的「0.4 术语锚点」 |
| 3 | 对应功能附录（L2）中的**状态行为映射表**（如果有） |
| 4 | 对应功能附录（L2）中的**表格或列表形式的详尽定义**（如“交互状态细则”表格） |
| 5 | 对应功能附录（L2）中的自然语言描述 |
| 6 | 技术约定附录（A03等） |
| 7 | 数据字典（D04） |
| 8 | 历史文档或示例代码 |

**冲突示例**：如果 `附录 B` 中自然语言说“GM 可编辑招募帖”，但 `附录 00_状态行为映射表.md` 中对应状态为“GM 只能关闭/删除，不能编辑”，则以**映射表**为准。

---

### 4. 代码生成规范

#### 4.1 组件与复用

*   **布局容器**：所有后台类页面（创作者后台、个人中心、资产库等）必须使用 `PageLayout` 组件，**不得手写独立布局**。
*   **空状态**：列表/搜索结果为空时，必须使用 `EmptyState` 组件，包含 SVG 占位图（来自 `icons.svg`）、标题、描述和操作按钮（如适用）。
*   **图标**：所有图标必须使用 `<SvgIcon name="icon-xxx" />`，**严禁使用 Emoji**。图标名参考 `附录 A04：SVG图标系统规范.md`。
*   **按钮**：主次顺序为“次操作在左，主操作在右”。主操作按钮背景色使用 `var(--btn-primary-bg)`。一个页面最多一个主操作。
*   **状态机**：涉及按钮状态（如申请加入、GM审批）必须使用对应附录中定义的 **Composable**（如 `useApplyButtonState`），禁止在组件内 `if-else` 手写状态判定。

#### 4.2 样式

*   **设计令牌**：所有颜色、间距、圆角、阴影必须使用设计令牌变量（`var(--color-*)`, `var(--space-*)`, `var(--radius-*)` 等）。**严禁硬编码** `#fff`、`#333`、`10px` 等裸值。
*   **作用域**：组件样式必须使用 `<style lang="scss" scoped>`。全局样式仅放在 `src/styles/` 目录。

#### 4.3 API 与网络

*   **封装层**：所有 HTTP 请求必须通过 `api/` 目录下封装的函数，组件内 **不得直接调用** `axios` 或 `fetch`。
*   **Socket.IO**：实时通信事件（跑团房间、私信）必须通过 `composables/useSocket.ts` 管理，组件内不得直接 `socket.emit`。
*   **幂等键**：所有写操作（POST/PUT/DELETE）的请求体中必须携带 `idempotent_key`（UUID v4 字符串），由前端生成。
*   **错误处理**：API 错误响应格式为 `{ success: false, error_code: string, message: string }`。前端必须处理常见错误码（如 `ROOM_FULL`, `PERMISSION_DENIED`）并给出用户提示。

#### 4.4 数据库与后端

*   **主键**：所有表主键使用 UUID v4（`VARCHAR(64)`），用户可见的公开 ID（如 UID、房间码）单独字段。
*   **JSON 字段**：扩展数据使用 `JSON` 类型字段（如 `extras`, `metadata`），避免频繁 ALTER TABLE。
*   **时间字段**：世界标准时间使用 `TIMESTAMP`，仅用于排序；剧情时间使用 `JSON` 格式 `{ day, hour, minute }`。

---

### 5. Fallback 行为（遇到不确定信息时）

如果你在文档中找不到某个必要信息，**禁止自行假设或编造**。请按以下方式处理：

| 缺失信息类型 | 输出内容 | 后续行动 |
|------------|---------|---------|
| 图标名称 | 输出 `<!-- TODO: 需要图标 {描述} -->`，并用一个简单的占位块（如灰色方块）代替。 | 等待人工提供图标名后替换。 |
| 状态机定义 | 输出 `// TODO: 状态 {状态名} 未在映射表中定义，请补充`，并采用最保守的禁用状态。 | 暂停该功能实现，优先完成映射表。 |
| API 字段名或类型 | 查阅 `附录 D04`，若仍未定义，输出 `// ASK: 字段 {字段用途} 请确认名称和类型`。 | 等待人工确认后再继续。 |
| 业务流程分支（如“超时后是否自动拒绝”） | 输出 `// ASK: 超时未确认的预期行为是什么？` | 等待人工确认。 |
| 权限判断（如“谁可以删除 NPC”） | 查阅功能附录中的权限矩阵，若无，输出 `// TODO: 权限待定义，当前仅允许 GM 操作`。 | 使用最严格的权限（仅 GM）作为临时实现，并标注待完善。 |

> **重要**：任何 Fallback 输出都必须包含明确的 `TODO` 或 `ASK` 标记，方便人工快速定位和补充。**不得**静默使用默认值或随机值。

---

### 6. 输出格式要求

*   当生成代码时，请使用 **代码块** 并标注语言（如 `vue`, `typescript`, `sql`）。
*   对于复杂的业务逻辑，优先使用 **表格** 或 **伪代码** 进行说明，而不是大段自然语言。
*   在代码注释中，如果某处实现参考了特定的附录或表格，请注明来源（例如 `// 状态参见 附录00_状态行为映射表.md 表1`）。

---

### 7. 自我检查清单（生成代码前）

在输出最终答案之前，请逐项确认：

- [ ] 变量名、路由、API 字段是否符合「0.4 术语锚点」？
- [ ] 按钮状态是否来自预定义的 Composable 或映射表？
- [ ] 颜色/间距是否使用了设计令牌变量，无硬编码？
- [ ] 是否复用了 `PageLayout`, `EmptyState`, `SvgIcon` 等全局组件？
- [ ] API 请求是否通过 `api/` 封装，写操作是否带了 `idempotent_key`？
- [ ] 错误处理是否至少覆盖了文档中定义的常见错误码？
- [ ] 如有不确定信息，是否已用 `TODO`/`ASK` 标记并采用了保守 Fallback？

---

## 0.1 2026-04-25 对齐声明（新增）

本文件遵循 `docs/文档对齐治理.md` 中定义的统一优先级与冲突处理规则。

本文额外约束如下：

- 规则工坊采用 Recipe 类型体系（`threshold_check`、`opposed_check`、`resource_modify`、`random_table`、`initiative`、`growth_check`、`accumulation`、`chain`、`raw`），历史文段中出现的“L3 可视化原子画布”视为已下线方案。
- 存储模型以“Recipe 源码 + compiled graph（atoms + connections）”为准；执行器持续只消费编译产物。
- 指令绑定入口由 `entry_atom` 语义迁移为 `recipe_id` + `runtime_modifiers` 注入。
- 视觉规范统一以附录 B / 附录 D 为准，旧有 `--tp-*` 等前缀命名全部视为废弃。
- 图标与空状态插画资产以 `docs/附录 S：SVG 图标系统规范.md` 和首页交付包的交付清单为准。
- 文中出现的 Emoji、字符图标和纯文字占位仅用于示意信息结构，实际实现必须替换为 SVG 资产体系。


## 0.2 2026-04-27 创作者入口与权限修缮（新增）

### 创作者入口信息架构

| 平台 | 入口路径 |
|------|---------|
| 桌面端 | 主顶栏右侧"创作台"按钮（`/creator`），仅创作者可见 |
| 移动端 | ① 顶栏 `icon-workshop` 图标；② 个人页首屏创作台入口卡片（位于用户信息卡与 Stats 卡之间） |
| 共同 | 底部导航固定 5 项，**不新增**第 6 项 |

### 权限闭环约定

- `subscription_type === 'creator'` 或 `user_type` 含 `'creator'`/`'admin'` 的用户视为创作者
- 前端：路由 `/creator/**` 均设置 `requiresCreator: true`；全局守卫非创作者跳转 `Forbidden`
- 后端：rulesets/modules 写接口接入 `requireCreator` 中间件；返回 403
- 创作者状态写入 `authStore.isCreator` 并缓存至 `localStorage.is_creator`；用户信息加载中时入口不显示（骨架态）

### 加载与异常处理

- 用户信息加载中：显示骨架占位卡片，不显示任何创作台入口
- 用户信息加载失败：显示错误提示与重试按钮，明确反馈而非静默吞错
- 创作台内容未完成模块（素材库/我的作品）：显示"建设中"标签与下一步引导

## 0.3 文档拆分迁移入口（新增）

为降低主文档维护复杂度，本文件按"总纲 + 功能附录"持续拆分。当前执行基线见：

- 《[附录 00：文档拆分与迁移执行清单](附录 00：文档拆分与迁移执行清单.md)》

本文件中的详细实现将逐步迁移到对应模块附录，迁移完成后主文档仅保留：

1. 业务范围与边界
2. 核心能力清单
3. 跨模块依赖说明
4. 附录索引与验收口径

迁移中如出现同名规则冲突，以模块附录中的最新定义为准，并在清单中登记版本与日期。

当前已建立的拆分入口：

- 《[附录 C：跑团房间交互设计](附录 C：跑团房间交互设计.md)》
- 《[附录 D01：规则引擎核心设计](附录 D01：规则引擎核心设计.md)》
- 《[附录 E：模组编辑器](附录 E：模组编辑器设计方案_v1.0.md)》

迁移治理共识（2026-04-28）：

1. `产品设计.md` 作为 L1 总纲，不再持续堆叠实现细节。
2. 功能附录（L2）为业务权威来源，子附录（L3）为可查询规范来源。
3. 历史内容保留为"历史/参考"，逐步出清重复定义。

## 0.4 术语锚点（统一口径）

为避免跨文档同义词并行维护，以下术语作为全局锚点，冲突时以本节为准：

1. 团 vs 房间：对外统一用“房间”（用户视角），数据模型统一用 `Campaign`（文档可写“房间（Campaign）”）。
2. 招募帖 vs 发车：统一用“招募帖”，“发车”不出现在任何控件、状态名和流程名中。
3. 场 vs 场景：统一用“场”（如剧情场、私密场、公共场）；历史文本中的“场景”逐步替换。
4. 角色卡 vs 人物卡：统一用“角色卡”。
5. KP vs GM vs 主持人：平台内统一用 `GM`；面向用户的产品文案可辅以“主持人”，但 API、表单、状态字段只使用 `GM`。

## 0.5 重复定义收敛规则（L2/L3）

1. 状态机只保留一份权威定义，位于对应功能附录（L2）。
2. 数据字典/表结构/Schema 只保留一份权威定义，位于 L3（`附录 D04`）。
3. 其他文档仅使用“章节引用”方式回链，不复制完整定义。
4. `产品设计.md` 只保留能力边界与索引，不写交互细节正文。


## 1. 术语定义与核心概念

### 1.1 基础术语

| 术语                      | 说明                                     | 示例                     |
| ----------------------- | -------------------------------------- | ---------------------- |
| **团（Campaign）**         | GM创建的顶层容器，包含剧情时间标签记录、所有场、成员列表、角色卡绑定      | "黑暗边缘团"                |
| **场（Scene）**            | 团内的子空间，分为剧情场、私密场、公共场三种类型               | "酒馆"、"刺客小队密谈"          |
| **剧情场（Spatial）**        | 真实叙事位置，玩家可移动至此，参与位置矩阵                  | "废弃教堂"、"警局档案室"         |
| **私密场（Virtual）**        | 秘密对话空间，不记录位置但记录时间线                     | "刺客小队"、"GM-张三密谈"       |
| **公共场（Lobby）**          | 非剧情闲聊空间，不参与时间同步                        | "银光酒馆大厅"               |
| **助理台（Assistant Desk）** | 固定置顶的个人沙盒面板，不视为场，测试掷骰不进log，GM暗骰记录仅自己可见 | 指令大全、暗骰记录（仅GM）         |
| **剧情时间标签**              | GM在聊天流中手动宣布的时间标记，格式 `HH:MM`（仅时、分）    | "08:30"            |
| **个人剧情时间**              | 角色进出场时 GM 可选填的剧情时间，仅用于轨迹矩阵与日志记录       | 角色A到达教堂时GM标记"08:30" |
| **轨迹矩阵**                | 可视化展示所有角色在各剧情场的时间分布图                   | 灰白分区表格                 |
| **写扩散**                | 消息发送时预计算可见角色列表并存储，避免查询时 JOIN 参与时间段表。                   | 灰白分区表格                 |
| **雪花 ID**                | 分布式唯一 ID 生成算法，保证消息 ID 全局单调递增，用于排序。                   | 灰白分区表格                 |

### 1.2 角色卡术语

| 术语 | 说明 |
|------|------|
| **角色卡（Character Sheet）** | 玩家创建的虚拟角色数据，包含属性、技能、资源 |
| **规则包（Ruleset）** | 定义角色卡结构、检定公式、派生计算的逻辑包 |
| **职业（Occupation/Class）** | 规则包定义的角色模板，影响属性成长和技能 |
| **派生值（Derived）** | 由基础属性自动计算的值（如HP、SAN、熟练加值） |

---

## 2. 规则工坊

> 本章详细内容已迁移至《[附录 D01：规则引擎核心设计](附录 D01：规则引擎核心设计.md)》。
> 主文档仅保留总纲索引，避免重复维护。

### 2.1 索引

1. Recipe、编译产物、指令体系与执行边界：见附录 D01。
2. 规则数据字典与 Schema 定义：见附录 附录 D04。
3. 配方系统技术方案与兼容策略：见附录 D03。

## 3. 模组编辑器

> 本章详细内容已迁移至《[附录 E：模组编辑器](附录 E：模组编辑器设计方案_v1.0.md)》。
> 主文档仅保留总纲索引，避免重复维护。

### 3.1 索引

1. 富文本 + Mention 编辑、发布与公示流程：见附录 附录 E。
2. 模组规则绑定、导入与导出能力：见附录 附录 E。
3. 模组数据结构与引用约定：见附录 附录 D04。

## 4. 跑团房间（重构版：基于多场时间同步）

> 本章详细内容已迁移至《[附录 C：跑团房间交互设计](附录 C：跑团房间交互设计.md)》。
> 主文档仅保留总纲索引，避免重复维护。

### 4.1 索引

1. 场、消息、移动与权限流：见附录 附录 C。
2. 招募成团、房间交互与日志导出：见附录 附录 C。
3. 房间数据结构与全局 Schema：见附录 附录 D04。

## 5. 角色卡系统

> 本章详细内容已迁移至《[附录 D05：角色卡系统产品设计](附录 D05：角色卡系统产品设计.md)》。
> 主文档仅保留总纲索引，避免重复维护。

### 5.1 索引

1. 角色卡创建、绑定、同步与归档：见附录 附录 D05。
2. 角色卡开放交换格式：见附录 B03。
3. 角色卡数据结构与 Schema：见附录 附录 D04。

## 6. 市场与创作者经济

> 本章详细内容已迁移至《[附录 H02：市场与创作者经济产品设计](附录 H02：市场与创作者经济产品设计.md)》。
> 主文档仅保留总纲索引，避免重复维护。

### 6.1 索引

1. 市场能力与创作者经济规则：见附录 H02。
2. 会员权益与商业化口径：见附录 H01。
3. 交易与数据结构：见附录 附录 D04。

## 7. 社区与社交

> 本章详细内容已迁移至《[附录 B：社区产品设计](附录 B：社区产品设计.md)》。
> 主文档仅保留总纲索引，避免重复维护。

### 7.1 索引

1. 社区主页、招募互动与评论体系：见附录 附录 B。
2. 招募与房间联动流程：见附录 附录 C。
3. 社区相关数据结构：见附录 附录 D04。

## 8. AI 功能（规划中，暂不实现）

> 本章详细内容已迁移至《[附录 I01：AI功能产品设计](附录 I01：AI功能产品设计.md)》。
> 主文档仅保留总纲索引，避免重复维护。

### 8.1 索引

1. AI 导入与 AI 校对能力：见附录 I01。
2. AI 安全与隐私规则：见附录 I01。
3. AI 相关数据结构与留痕：见附录 附录 D04。

## 9. 账号与基础设施

> 本章详细内容已迁移至《[附录 I02：账号与基础设施产品设计](附录 I02：账号与基础设施产品设计.md)》。
> 主文档仅保留总纲索引，避免重复维护。

### 9.1 索引

1. 账号、身份、登录态与安全责任：见附录 I02。
2. 部署与基础设施配置：见附录 I02。
3. 账号相关数据结构：见附录 附录 D04。

## 10. 法律与合规

> 本章详细内容已迁移至《[附录 I03：法律与合规产品设计](附录 I03：法律与合规产品设计.md)》。
> 主文档仅保留总纲索引，避免重复维护。

### 10.1 索引

1. 协议、隐私、侵权投诉与内容审核：见附录 I03。
2. 版权存证、内容分级与未成年人提示：见附录 I03。
3. 账号相关合规衔接：见附录 I02。


## 11. 业务安全与核心信誉保障

> 本章详细内容已迁移至《[附录 I04：业务安全与核心信誉保障产品设计](附录 I04：业务安全与核心信誉保障产品设计.md)》。
> 主文档仅保留总纲索引，避免重复维护。

### 11.1 索引

1. 掷骰可信、防篡改与审计追溯：见附录 I04。
2. 引擎沙箱与执行安全边界：见附录 I04。
3. 错误态、空状态与权限态视觉规范：见附录 I04。
  
## 12. 货币系统（阶段一引入，MVP 预留）

> 本章详细内容已迁移至《[附录 H03：货币系统产品设计](附录 H03：货币系统产品设计.md)》。
> 主文档仅保留总纲索引，避免重复维护。

### 12.1 索引

1. 货币字段预留、运营参数与配置化方案：见附录 H03。
2. 积分获取、消耗、兑换与公测过渡规则：见附录 H03。
3. 会员兑换相关口径：见附录 H03，并与附录 H01 联动。

---
## 13. 商业化与支付系统

> 本章详细内容已迁移至《[附录 H04：商业化与支付系统产品设计](附录 H04：商业化与支付系统产品设计.md)》。
> 主文档仅保留总纲索引，避免重复维护。

### 13.1 索引

1. 支付接入、订单结算、资金流与支付安全：见附录 H04。
2. 创作者分成、提现、税务与实名认证：见附录 H04。
3. 会员权益细则、货币规则与市场上下文：分别见附录 H01、附录 H03、附录 H02。




## 五、关键交互流程

### 13.2 简化开团流程

当用户从首页"快速组车卡片"或资产库"已拥有模组/规则包"点击 `[开团]` 时，系统将：
1. 自动创建私密团并按入口资产类型落库：
  - 从“模组资产”进入：绑定 `module_id` 与对应 `ruleset_id`。
  - 从“规则包资产”进入（无模组）：`module_id = null`，仅绑定 `ruleset_id`，创建空白团。
2. 跳过房间名填写、招募选项等步骤，直接进入房间。
3. 房间默认名规则：有模组时为"{模组名} 的团"；无模组时为"{规则包名} 的团"。GM 可后续在房间设置中修改。

此流程旨在将"从拥有资产到开始游戏"的点击次数降至最低。

### 13.3 用户目标路径汇总

| 用户目标 | 路径 | 点击次数 |
|----------|------|----------|
| 开一个私密团（和朋友玩） | 资产库点击`[开团]` → 填房间名 → 创建 | 2次（含表单确认） |
| 开一个公开招募团 | 资产库点击`[开团]` → 填房间名 + 勾选"公开招募" → 创建 → 跳转招募板 → 补充信息发布 | 4-5次 |
| GM招募玩家 | 社区→招募板→发布招募帖 → 填写表单 → 发布 | 3-4次 |
| 玩家找团 | 社区→招募板→找团玩 → 筛选 → 申请加入 | 3次 |
| GM邀请求组玩家 | 社区→招募板→当GM → 点击`[邀请入团]` → 选择现有/新建房间 → 发送 | 3次 |
| 玩家接受邀请 | 收到通知 → 点击接受 | 2次 |
| 观看一场公开招募团的游戏 | 社区→招募板→找团玩 → 筛选"可观战" → 点击`[观看直播]` | 3-4次 |

---


好的，我将为你生成 **「状态-行为映射表」**，作为AI查询的唯一来源。我会整合现有文档中的状态机定义，并以清晰、可检索的Markdown表格形式呈现。

我会按照以下结构生成每个表格：
- **场景**：描述该表适用的业务上下文
- **状态/条件**：用精确的枚举值或条件表达式表示
- **UI文案/行为**：用户看到的文字或系统行为
- **操作(action)**：前端应触发的动作标识（可映射到API或UI跳转）
- **后端响应/API**：对应的接口或Socket事件
- **附注**：补充说明如倒计时、候补位、权限等

---

## 状态-行为映射表集

### 表1：招募帖卡片按钮状态

**场景**：社区招募板中，“找团玩”Tab下的GM招募帖卡片上的操作按钮。

| 索引键(供AI查找) | 状态/条件 | UI文案 | 按钮禁用? | 操作(action) | 后端响应/API | 附注 |
|-----------------|----------|--------|----------|--------------|-------------|------|
| `not_logged_in` | 未登录 | 申请加入 | 否 | `showLogin` | - | 弹出登录/注册模态框 |
| `gm_self` | 当前用户是发帖GM | 管理招募 | 否 | `navigateToManage` | `GET /recruitment/{id}/manage` | 跳转至招募帖管理页 |
| `applied_pending` | 已申请，状态=`pending` | 已申请 | 是 | - | - | 按钮灰色 |
| `applied_invited` | GM已通过，状态=`invited` | 确认入团 (Xh Ym) | 否 | `confirmApplication` | `POST /applications/{id}/confirm` | 需显示剩余确认时长(倒计时) |
| `applied_confirmed` | 已确认入团，状态=`confirmed` | 已加入 | 是 | - | - | 绿色对勾图标 |
| `applied_waiting` | 候补中，状态=`waiting` | 候补中 (第N位) | 是 | - | - | 显示候补序号 |
| `applied_rejected` | 申请被拒，状态=`rejected` | 申请被拒 | 是 | `viewReason` | - | 点击可查看拒绝原因 |
| `recruit_open_slots` | 招募状态=`open`，剩余席位>0 | 申请加入 | 否 | `apply` | `POST /recruitment/{id}/apply` | 正常申请流程 |
| `recruit_open_full` | 招募状态=`open`，剩余席位=0 | 加入候补 | 否 | `applyWaitlist` | `POST /recruitment/{id}/apply?type=waiting` | 仅当允许候补时显示 |
| `recruit_grouped_spectate` | 招募状态=`grouped`，可观战=`true` | 观看直播 | 否 | `spectate` | `GET /room/{campaign_id}?mode=spectate` | 以OB身份进入房间 |
| `recruit_grouped_no_spectate` | 招募状态=`grouped`，可观战=`false` | 已满员 | 是 | - | - | - |
| `recruit_closed` | 招募状态=`closed`/`dissolved`/`archived` | 已结束 | 是 | - | - | - |

---

### 表2：申请加入流程（玩家视角）

**场景**：玩家在招募帖点击“申请加入”后的状态流转。

| 当前状态 | 触发条件 | 系统行为 | 下一状态 | 通知/反馈 | 可执行操作 |
|---------|---------|---------|---------|----------|-----------|
| `none` | 玩家点击“申请加入” | 创建申请记录，状态=`pending` | `pending` | 前端按钮变为“已申请” | 无(不可取消) |
| `pending` | GM通过申请 | 更新申请状态=`invited`，设定邀请有效期(24h) | `invited` | 发送站内信+Socket通知玩家 | 玩家可点击“确认入团” |
| `invited` | 玩家点击“确认入团” | 更新状态=`confirmed`，占席位 | `confirmed` | GM端收到确认通知 | 无 |
| `invited` | 超时未确认(24h) | 系统自动更新状态=`rejected`，释放席位 | `rejected` | 通知玩家超时拒绝 | 无 |
| `pending` | GM拒绝 | 更新状态=`rejected`，可选填写拒绝原因 | `rejected` | 通知玩家被拒 | 玩家可查看原因 |
| `waiting` | 席位空出，等待队列首位 | 自动推进为`invited`，发送邀请 | `invited` | 通知玩家“已从候补转正”，开始确认倒计时 | 玩家可“确认入团” |
| `confirmed` | GM成团 | 状态不再变化 | `confirmed` | 通知玩家房间已创建 | 玩家可进入房间 |

---

### 表3：GM审批移动弹窗

**场景**：GM在跑团房间审批玩家预约移动时的交互。

| GM操作 | 条件 | 前端弹窗行为 | 后端动作 | 预约移动状态 | 玩家端反馈 |
|-------|------|------------|---------|------------|-----------|
| 点击“批准” | 未填写剧情到达时间 | 弹出确认框，预设到达时间=当前全局时间+预计耗时(若有连通) | 立即执行移动：更新角色位置、记录位置历史(story_time_entered=NULL)、发送`scene_event`消息 | `executed` | 聊天区显示“角色名 离开 A，前往 B” |
| 点击“批准” | 填写了剧情到达时间 | 弹窗确认后关闭 | 将预约状态改为`approved`，写入`execute_at_story` | `approved` | 通知玩家“移动已批准，将于X时间自动执行” |
| 点击“拒绝” | - | 弹出原因填写框(可选) | 状态改为`cancelled`，记录拒绝原因(如有) | `cancelled` | 发送`move_rejected`通知，含原因(若有) |
| 无法审批 | 目标场景`status != active` | “批准”按钮禁用，显示红色警告“目标场景不可用” | 无操作 | - | - |
| GM强制移动 | 右键角色 -> “强制移动” | 弹窗选择目标场景，选择“立即执行”或“设定剧情时间” | 立即执行或创建已批准预约(跳过玩家申请) | 新预约或直接执行 | 聊天区显示`[GM强制]`消息 |

---

### 表4：场景管理操作（GM）

**场景**：GM在剧本模式或GM控制台中对场景进行生命周期管理。

| 操作 | 二次确认弹窗 | 前端反馈 | 后端动作 | 返回值/异常 | 适用状态 |
|-----|------------|---------|---------|------------|---------|
| 归档场景 | 是：“归档后场景不可进入，剧情历史保留” | 场景在列表中变为“已归档”灰色标记，不可进入 | `scenes.status` → `archived` | 成功 | `active` → `archived` |
| 隐藏场景 | 是：“隐藏后玩家不可见，仅GM保留” | 玩家端场景消失，GM端显示“(隐藏)” | `scenes.status` → `hidden` | 成功 | `active` → `hidden` |
| 恢复场景 | 否 | 场景恢复正常显示 | `scenes.status` → `active` | 成功 | `archived`/`hidden` → `active` |
| 删除场景 | 是：“此操作不可逆。若有历史数据则只能归档” | 若满足删除条件则删除，否则提示并禁用删除 | 物理删除(同时满足：消息数=0、轨迹数=0、从未被进入、创建<24h) | 成功或错误(SCENE_DELETE_PROTECTED) | 仅新建且无数据时可用 |

---

### 表5：消息发送失败处理

**场景**：聊天消息发送过程中客户端的状态与重试逻辑。

| 消息状态 | UI表现 | 用户操作 | 重试策略 | 超时/失败处理 |
|---------|-------|---------|---------|-------------|
| `pending` | 气泡半透明(opacity:0.6)，左侧显示加载动画(16px) | 不可操作 | 自动重试：1s、3s、7s，最多3次 | 3次全失败 → `failed` |
| `sent` | 正常气泡，右侧显示绿色对勾(12px，可选) | 无 | - | - |
| `failed` | 气泡背景微红(`var(--color-danger-bg)`)，右下角红色感叹号(20px) | 点击感叹号弹出菜单：“重新发送” / “删除” | 手动重试立即发送一次 | 再次失败仍保持`failed` |
| `retrying` | 同`pending` | 不可操作 | 手动重试时重置计数 | - |
| 顶部横幅 | 有失败消息时顶部显示：“有N条消息发送失败 [查看] [全部重试] [✕]” | 点击查看可定位到失败消息 | 全部重试对每条手动重试 | 关闭横幅不影响消息状态 |

---

### 表6：软删除占位符（评论系统）

**场景**：评论区主楼层或楼中楼被删除后的占位显示。

| 被删对象类型 | 删除者 | 占位UI文案 | 占位区域样式 | 是否保留楼层号 | 操作按钮 |
|------------|-------|-----------|------------|-------------|---------|
| 主楼层(楼层) | 管理员(admin) 或 GM | `[该楼层已被管理员删除]` | 灰色背景，高度48px，居中灰字14px | 是，仍显示“X楼” | 全部隐藏(点赞/回复/举报) |
| 主楼层(楼层) | 作者本人(author) | `[该回复已被作者删除]` | 同上 | 是 | 全部隐藏 |
| 楼中楼 | 任何有权限者(admin/gm/author) | `[该回复已被删除]` | 40px高灰色块，内含灰字 | 不适用 | 不显示任何操作按钮 |

**AI注意**：占位区域不可点击、不可展开、不可选中。所有占位文案必须严格使用上述文字，不得自创。

---

### 表7：团（Campaign）生命周期状态机

| 当前状态 | 允许转换到 | 触发条件 | 系统行为 | 前端反馈 |
|---------|-----------|---------|---------|---------|
| `preparing` | `running` | GM点击“开始游戏” | 更新`campaigns.status`, 通知所有成员 | 房间界面变为“进行中” |
| `preparing` | `ended` | GM解散房间 | 标记结束，成员退出 | 房间移至“已结束”列表 |
| `running` | `paused` | GM暂停 | 暂停消息广播，禁止新消息/移动 | 聊天区显示“游戏已暂停” |
| `paused` | `running` | GM恢复 | 恢复所有交互 | 聊天区显示“游戏已恢复” |
| `running` / `paused` | `ended` | GM结束团 | 归档团数据，释放所有角色卡绑定 | 房间只读，成员可导出日志 |

---

### 表8：招募帖状态机

| 当前状态 | 允许转换到 | 触发条件 | 系统行为 | 可用操作 |
|---------|-----------|---------|---------|---------|
| `draft` | `open` | 用户点击“发布” | 状态变为`open`，开始接受申请 | 发布、编辑、删除 |
| `open` | `full` | 确认人数达到上限(`confirmed`玩家数 = max) | 自动状态变更，前端显示“满员” | 新申请进入候补 |
| `full` | `open` | 有成员退出或超时未确认导致席位释放 | 自动状态变更 | 继续接受申请 |
| `open`/`full` | `grouped` | GM点击“成团”成功 | 创建房间，关联`campaign_id` | 不再接受申请 |
| `open`/`full` | `closed` | GM手动关闭或截止时间到 | 状态变为`closed` | 不可申请，只读 |
| `grouped` | `dissolved` | GM解散房间或系统自动(7天无人进入/超50%退出) | 团数据归档，招募帖标记为`dissolved` | 可重新开团(复制原帖) |
| `grouped`/`closed` | `archived` | 超30天自动归档 | 移至历史归档 | 只读，不可互动 |

---

### 表9：预约移动状态机

| 当前状态 | 允许转换到 | 触发条件 | 系统行为 | 触发通知 |
|---------|-----------|---------|---------|---------|
| `pending` | `approved` | GM批准并填写到达时间 | 写入`execute_at_story` | 通知玩家：已批准，将于X时间执行 |
| `pending` | `executed` | GM批准时未填时间(立即执行) | 立即执行移动，更新位置 | 发送`position_changed`事件 |
| `pending` | `cancelled` | 玩家自行取消 | 删除预约 | 通知GM：已取消 |
| `pending` | `cancelled` | GM拒绝 | 记录拒绝原因(可选) | 通知玩家被拒(含原因) |
| `approved` | `executed` | 全局时间推进，当前时间>=`execute_at_story` | 自动执行移动，更新位置历史 | 广播移动事件 |
| `approved` | `cancelled` | GM手动取消 | 状态变更 | 通知玩家取消 |
| `approved` | `cancelled` | 目标场景`status != active` | 自动取消并发通知 | 通知玩家“目标场景已关闭” |

---

### 表10：模组/规则包审核状态机

| 当前状态 | 允许转换到 | 触发条件 | 系统行为 | 创作者操作 |
|---------|-----------|---------|---------|-----------|
| `draft` | `reviewing` | 创作者提交审核 | 创建审核记录，进入排队 | 不可再编辑 |
| `reviewing` | `public_notice` | 平台审核通过 | 进入7天公示期 | 不可编辑，可查看异议 |
| `reviewing` | `draft` | 平台驳回 | 退回草稿，填写驳回理由 | 可修改后重新提交 |
| `public_notice` | `published` | 公示期满7天，无有效异议 | 上架市场，可被购买/引用 | 可见销量数据 |
| `public_notice` | `suspended` | 收到有效侵权投诉 | 下架，等待申诉 | 可提交申诉材料 |
| `published` | `suspended` | 后续被投诉且核实 | 下架，冻结分成 | 可申诉 |
| `suspended` | `published` | 申诉成功 | 恢复上架 | 正常销售 |

---

以上10张表格覆盖了平台核心业务的状态机，且每行都是独立的原子规则，便于AI直接查询和适配。你可以将它们整理成一个独立的Markdown文件（如 `附录00_状态行为映射表.md`），放在文档根目录，并在「AI执行指南」中指明此为状态机唯一来源。

接下来，如果你需要我补充其他映射表（如“用户信誉分等级影响权限”）或生成其他优先项（AI执行指南/伪代码等），请告诉我。


## 附录：文档变更记录

表格

| 版本   | 日期         | 变更说明                                                                                                                                                                                               |
| :--- | :--------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V1.0 | 2026-03-24 | 初始版本，涵盖 MVP-α/β 及阶段一/二全部功能点                                                                                                                                                                        |
| V1.1 | 2026-03-26 | 移除本地优先架构，全面采用纯云端；简化MVP功能范围                                                                                                                                                                         |
| V1.2 | 2026-04-04 | **整合版**：融合多场时间同步架构（团-场体系、轨迹矩阵、预约移动）与角色卡系统；重构跑团房间章节；统一数据库设计；明确角色卡与场的绑定关系                                                                                                                           |
| V1.3 | 2026-04-11 | 整合时间同步技术方案：增加消息写扩散机制、修正预约移动事务描述、补充数据库表字段、新增附录A03技术细则                                                                                                                                               |
| V1.4 | 2026-04-24 | **功能完善**：拆分消息类型枚举（新增 `scene_event`、`gm_ledger`）；新增场生命周期管理（active/archived/hidden 三态 + 物理删除保护）；移动过渡消息按场隔离生成（信息隔离原则）；新增预约移动自动调度（时间触发 + 冲突处理）；新增时间单调递增校验（禁止回退）；完善历史消息时序裁剪规则；日志导出新增消息类型过滤（OOC 默认不导出） |
| v1.5 | 2026-04-25 | **设计对齐版**：以根目录 6 份文档为绝对准则；规则工坊统一收口为 Recipe 配方体系；视觉与图标资产统一服从附录 B、附录 D、首页视觉修正文档、附录Q和 SVG 资产规范 |



